name: Apply Chapter 05 final publication review once

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/ch05-final-publication-review-once.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply Chapter 05 publication review
        run: python scripts/ch05_final_publication_review_once.py
      - name: Remove one-off workflow
        run: rm -f .github/workflows/ch05-final-publication-review-once.yml
      - name: Commit reviewed Chapter 05
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          if git diff --cached --quiet; then
            echo 'No changes to commit.'
            exit 0
          fi
          git commit -m 'Refine Chapter 05 final publication content'
          git push
