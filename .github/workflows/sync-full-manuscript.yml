name: Sync full manuscript

on:
  push:
    branches:
      - main
    paths:
      - "book/chapter*/chapter*.md"
      - "scripts/normalize_chapter_headers.py"
      - "scripts/merge_chapters.py"
      - ".github/workflows/sync-full-manuscript.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Normalize chapter headers
        run: python scripts/normalize_chapter_headers.py

      - name: Generate full manuscript
        run: python scripts/merge_chapters.py

      - name: Commit normalized chapters and manuscript
        shell: bash
        run: |
          if git diff --quiet -- book publish/full_manuscript.md; then
            echo "Chapter headers and full_manuscript.md are already up to date."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add book publish/full_manuscript.md
          git commit -m "장 머리말 형식 통일 및 통합 원고 재생성"
          git push
