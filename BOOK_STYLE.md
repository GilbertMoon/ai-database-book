# BOOK_STYLE.md

## AI 시대의 데이터베이스 입문 집필 스타일 가이드

이 문서는 **AI 시대의 데이터베이스 입문** 교재의 문체, 구성, 실습 코드, 도식, 표, 용어, 평가 문항, AI 활용 방식에 대한 공통 기준을 정의합니다.

모든 Chapter 원고, 실습 코드, 도식, 과제, 프로젝트 문서는 이 가이드를 기준으로 작성합니다.

---

## 1. 책의 기본 방향

### 1.1 책의 성격

이 책은 다음 성격을 갖는 교재입니다.

```text
- 실습형 eBook
- 대학 데이터베이스 입문 교재
- 일반 학습자용 자기주도 학습서
- ChatGPT와 Codex를 활용한 AI 시대형 DB 학습 교재
- PostgreSQL 기반 실습 교재
- DB 설계와 SQL 검증 중심 교재
```

### 1.2 핵심 메시지

이 책의 핵심 메시지는 다음과 같습니다.

```text
데이터베이스는 AI가 대신 만들어 주는 대상이 아니라,
AI가 만든 결과를 사람이 이해하고 검증해야 하는 핵심 기반 기술이다.
```

따라서 이 책은 단순히 SQL 문법을 외우는 방식이 아니라, 다음 흐름을 강조합니다.

```text
개념 이해
→ 직접 실습
→ AI에게 생성 요청
→ AI 결과 검토
→ 오류 수정
→ 실행 가능한 미니 프로젝트 완성
```

---

## 2. 대상 독자 수준

### 2.1 기본 독자

이 책의 기본 독자는 데이터베이스를 처음 배우는 학습자입니다.

```text
- 데이터베이스를 처음 배우는 대학생
- SQL을 처음 배우는 일반 학습자
- 개발 입문자
- AI 도구로 개발을 시작하려는 학습자
- DB 설계 경험이 부족한 비전공자 또는 초급 개발자
```

### 2.2 가정하는 사전 지식

다음 지식은 없어도 이해할 수 있도록 설명합니다.

```text
- 데이터베이스 설계 경험
- SQL 실무 경험
- 백엔드 개발 경험
- 클라우드 배포 경험
- Vector DB 또는 RAG 경험
```

다만 다음 정도의 기본 경험은 있으면 좋습니다.

```text
- 컴퓨터 파일과 폴더 개념
- 웹 서비스 사용 경험
- 간단한 코딩 실습 경험
- GitHub 계정 사용 경험
```

---

## 3. 문체 기준

### 3.1 기본 문체

문체는 **강의식, 친절한 설명, 실무형 예시 중심**으로 작성합니다.

권장 문체:

```text
- 짧고 명확한 문장
- 어려운 용어는 먼저 쉬운 말로 설명
- 개념 설명 후 바로 예시 제시
- 실습 전에는 왜 하는지 먼저 설명
- 실습 후에는 무엇을 확인해야 하는지 정리
```

피해야 할 문체:

```text
- 지나치게 학술적인 문장
- 정의만 나열하는 방식
- 초급자에게 부담스러운 장문 설명
- 도구 이름과 기술 용어를 설명 없이 나열하는 방식
- 실습 코드만 제시하고 해설하지 않는 방식
```

### 3.2 독자에게 말하는 방식

독자에게는 직접 말하듯 설명합니다.

권장 표현:

```text
이제 students 테이블을 만들어 보겠습니다.
여기서 중요한 점은 id 컬럼이 각 학생을 구분한다는 것입니다.
실행 결과에서 total_count 값이 어떻게 나오는지 확인해 보세요.
```

피해야 할 표현:

```text
상기 쿼리는 관계 대수적 관점에서 연산을 수행한다.
사용자는 이를 숙지하여야 한다.
```

---

## 4. Chapter 기본 구조

모든 Chapter는 가능하면 다음 구조를 따릅니다.

```text
1. 이 장에서 배울 내용
2. 왜 이 내용을 배우는가
3. 핵심 개념 설명
4. 작은 예제
5. 실습 준비
6. 실습 단계
7. 실행 결과 확인
8. AI 활용 실습
9. 자주 하는 실수
10. 정리
11. 연습 문제
12. 다음 장으로 연결
```

### 4.1 Chapter 시작부

각 Chapter의 시작에는 다음 정보를 포함합니다.

```markdown
## 이 장에서 배울 내용

이 장에서는 다음 내용을 배웁니다.

- 핵심 개념 1
- 핵심 개념 2
- 실습 내용 1
- AI 활용 포인트 1
```

### 4.2 Chapter 마무리

각 Chapter의 마지막에는 다음 항목을 포함합니다.

```markdown
## 정리

이번 장에서는 다음을 배웠습니다.

- 핵심 정리 1
- 핵심 정리 2
- 실습에서 확인한 내용

## 연습 문제

1. 개념 확인 문제
2. SQL 작성 문제
3. AI 결과 검토 문제

## 다음 장에서는

다음 장에서는 ...을 배웁니다.
```

---

## 5. 개념 설명 규칙

### 5.1 쉬운 정의를 먼저 제시

새로운 개념은 반드시 쉬운 정의를 먼저 제시합니다.

예시:

```text
테이블은 데이터를 행과 열로 정리한 구조입니다.
엑셀 시트와 비슷하지만, 데이터의 정확성과 관계를 더 엄격하게 관리할 수 있습니다.
```

그다음 기술적 정의를 추가합니다.

```text
관계형 데이터베이스에서 테이블은 하나의 엔티티 또는 관계를 표현하는 기본 저장 단위입니다.
```

### 5.2 예시는 생활 예시에서 시작

초급 개념은 다음과 같은 생활 예시를 우선 사용합니다.

```text
학생 관리
도서 대여
온라인 강의 수강신청
쇼핑몰 주문
병원 예약
게시판
```

복잡한 금융, 분산 시스템, 대규모 로그 처리 예시는 후반부에서만 사용합니다.

---

## 6. 코드 블록 작성 규칙

### 6.1 코드 블록에는 목적 설명을 먼저 둔다

코드만 먼저 제시하지 않습니다.

권장 형식:

```markdown
다음 SQL은 학생 정보를 저장할 `students` 테이블을 생성합니다.

```sql
CREATE TABLE students (
    id SERIAL PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL
);
```

이 SQL에서 `id`는 학생을 구분하는 기본키입니다.
```

### 6.2 코드 길이 기준

한 번에 제시하는 코드는 가능하면 짧게 유지합니다.

```text
- 초급 설명 코드: 10~20줄 권장
- 실습 코드: 30~60줄 가능
- 전체 프로젝트 코드: 파일 단위로 분리
```

긴 코드는 본문에 모두 넣지 않고, 저장소의 `code/chapterXX/` 경로를 안내합니다.

### 6.3 코드 설명 방식

코드 설명은 한 줄씩 모두 설명하기보다 실행 흐름 중심으로 설명합니다.

권장:

```text
이 코드는 크게 세 부분으로 구성됩니다.
첫째, DB 연결을 설정합니다.
둘째, students 테이블에서 데이터를 조회합니다.
셋째, 조회 결과를 HTML 화면에 전달합니다.
```

---

## 7. SQL 작성 규칙

### 7.1 SQL 키워드 표기

SQL 키워드는 대문자로 작성합니다.

권장:

```sql
SELECT name, email
FROM students
WHERE status = 'active'
ORDER BY name;
```

피함:

```sql
select name, email from students where status = 'active';
```

### 7.2 테이블명과 컬럼명

테이블명과 컬럼명은 다음 규칙을 따릅니다.

```text
- 테이블명: 복수형 권장
- 컬럼명: snake_case 사용
- 기본키: id 또는 table_name_id
- 외래키: 참조 대상 단수형_id
```

예시:

```text
students
courses
enrollments
student_id
course_id
created_at
updated_at
```

### 7.3 SQL 예제는 PostgreSQL 기준

모든 SQL은 기본적으로 PostgreSQL 기준으로 작성합니다.

```text
- AUTO_INCREMENT 대신 SERIAL 또는 GENERATED 사용
- DATETIME 대신 TIMESTAMP 사용
- 문자열은 VARCHAR 또는 TEXT 사용
- 불리언은 BOOLEAN 사용
```

---

## 8. 실습 환경 표기 규칙

### 8.1 기본 실습 환경

책의 기본 실습 환경은 다음으로 통일합니다.

```text
- PostgreSQL
- DBeaver
- GitHub
- ChatGPT
- Codex
- FastAPI 또는 Next.js
- Vercel
- Neon PostgreSQL 또는 Supabase PostgreSQL
```

### 8.2 로컬 우선, 클라우드 보조

기본 설명은 로컬 환경 기준으로 작성합니다.

```text
기본: 로컬 PostgreSQL + DBeaver
대안: Neon 또는 Supabase 무료 PostgreSQL
배포: Vercel + Neon PostgreSQL
```

### 8.3 설치 설명 기준

설치 설명은 운영체제별로 너무 길게 분기하지 않습니다.

권장 방식:

```text
- Windows 기준 설명을 기본으로 제공
- macOS/Linux는 차이점만 짧게 언급
- 설치가 어려운 학습자를 위해 클라우드 DB 대안 제공
```

---

## 9. AI 활용 설명 규칙

### 9.1 ChatGPT의 역할

ChatGPT는 주로 다음 용도로 설명합니다.

```text
- 요구사항 정리
- ERD 초안 생성
- SQL 초안 생성
- 오류 원인 설명
- 설계 대안 비교
- 학습 내용 요약
```

### 9.2 Codex의 역할

Codex는 주로 다음 용도로 설명합니다.

```text
- SQL 파일 생성
- FastAPI 또는 Next.js 코드 생성
- CRUD 기능 구현
- 테스트 코드 생성
- 코드 수정과 리팩토링
- GitHub 저장소 기반 작업
```

### 9.3 AI 결과는 항상 검증한다

AI가 만든 결과는 그대로 정답으로 제시하지 않습니다.

반드시 다음 흐름을 포함합니다.

```text
AI에게 요청
→ 생성 결과 확인
→ 문제점 찾기
→ 수정 방향 설명
→ 수정 결과 실행
```

### 9.4 AI 프롬프트 표기 방식

프롬프트는 다음 형식으로 제시합니다.

```markdown
다음 프롬프트를 ChatGPT에 입력해 보세요.

```text
온라인 강의 수강신청 시스템을 위한 데이터베이스 테이블을 설계해 주세요.
학생, 강의, 강사, 수강신청 정보를 포함하고,
각 테이블의 기본키와 외래키를 명확히 표시해 주세요.
```
```

프롬프트 뒤에는 반드시 검토 질문을 추가합니다.

```text
검토 질문:
- N:M 관계가 제대로 분리되었는가?
- 외래키가 빠진 테이블은 없는가?
- 중복 저장되는 컬럼은 없는가?
```

---

## 10. 도식 작성 규칙

### 10.1 도식이 필요한 경우

다음 내용은 가능하면 도식을 포함합니다.

```text
- DBMS와 애플리케이션 관계
- 테이블 구조
- PK/FK 관계
- ERD
- JOIN 동작 방식
- 트랜잭션 흐름
- 인덱스 개념
- RDBMS vs NoSQL 비교
- Vector DB와 RAG 흐름
- 웹 CRUD 아키텍처
```

### 10.2 도식 캡션 규칙

도식에는 반드시 번호와 설명을 붙입니다.

```markdown
![학생과 수강신청 테이블 관계](../../images/chapter05/student_enrollment_erd.png)

그림 5-1 학생과 수강신청 테이블의 관계
```

### 10.3 도식 파일명 규칙

도식 파일명은 다음 규칙을 따릅니다.

```text
chapter번호_주제_설명.png
```

예시:

```text
ch05_student_enrollment_erd.png
ch09_transaction_flow.png
ch14_rag_vector_search_flow.png
```

---

## 11. 표 작성 규칙

### 11.1 비교 설명은 표를 적극 활용

다음 내용은 표로 정리합니다.

```text
- RDBMS와 NoSQL 비교
- PostgreSQL과 MySQL 비교
- 기본키와 외래키 비교
- JOIN 종류 비교
- 인덱스 장단점
- AI 도구별 역할 비교
```

### 11.2 표는 너무 크지 않게 작성

한 표는 가능하면 4~6행, 3~5열 안에서 작성합니다.

큰 표는 여러 개의 작은 표로 분리합니다.

---

## 12. 용어 표기 규칙

### 12.1 한글과 영문 병기

처음 등장하는 핵심 용어는 한글과 영문을 함께 표기합니다.

예시:

```text
기본키(Primary Key, PK)
외래키(Foreign Key, FK)
트랜잭션(Transaction)
인덱스(Index)
벡터 데이터베이스(Vector Database)
검색 증강 생성(Retrieval-Augmented Generation, RAG)
```

이후에는 한글 또는 약어를 사용해도 됩니다.

### 12.2 주요 용어 표기 통일

| 개념 | 표기 |
| --- | --- |
| Database | 데이터베이스 |
| DBMS | DBMS |
| RDBMS | 관계형 DBMS 또는 RDBMS |
| PostgreSQL | PostgreSQL |
| Postgres | Postgres |
| DBeaver | DBeaver |
| Primary Key | 기본키(PK) |
| Foreign Key | 외래키(FK) |
| CRUD | CRUD |
| Vector DB | Vector DB 또는 벡터 데이터베이스 |
| RAG | RAG |
| Vibe Coding | 바이브코딩 |
| ChatGPT | ChatGPT |
| Codex | Codex |

---

## 13. 보안 관련 표현 규칙

### 13.1 위험한 코드는 반드시 위험하다고 표시

SQL Injection 등 위험한 코드를 설명할 때는 반드시 경고를 표시합니다.

```markdown
> 주의: 아래 코드는 SQL Injection에 취약한 예시입니다. 실제 서비스에서는 사용하면 안 됩니다.
```

### 13.2 안전한 대안을 함께 제시

위험한 예시만 제시하지 않고, 반드시 안전한 예시를 함께 제공합니다.

```text
위험한 방식
→ 왜 위험한가
→ 안전한 방식
→ 실습에서 확인할 점
```

### 13.3 개인정보 예시는 가상 데이터만 사용

실습 데이터는 반드시 가상 데이터를 사용합니다.

```text
홍길동, 김민지, 이준호 등 예시 이름 사용 가능
실제 전화번호, 이메일, 주민등록번호 사용 금지
```

---

## 14. 실습 코드 범위

### 14.1 초급자 기준으로 실행 가능해야 한다

실습 코드는 다음 기준을 만족해야 합니다.

```text
- 복사해서 실행 가능해야 한다.
- 실행 전 준비 조건을 명확히 안내해야 한다.
- 실행 결과를 본문에 설명해야 한다.
- 오류가 날 수 있는 지점을 미리 알려야 한다.
```

### 14.2 웹 실습은 최소 CRUD 중심

웹 실습은 기능을 과도하게 늘리지 않습니다.

기본 기능:

```text
- 목록 조회
- 상세 조회
- 등록
- 수정
- 삭제
- 간단한 검색
```

인증, 결제, 파일 업로드, 실시간 기능은 최종 프로젝트의 선택 확장 기능으로만 다룹니다.

---

## 15. 연습 문제 작성 규칙

### 15.1 문제 유형

각 Chapter의 연습 문제는 다음 유형을 섞어서 구성합니다.

```text
- 개념 확인 문제
- SQL 작성 문제
- 실행 결과 예측 문제
- 오류 찾기 문제
- AI 생성 결과 검토 문제
- 미니 실습 확장 문제
```

### 15.2 난이도 구분

문제는 가능하면 난이도를 표시합니다.

```text
기초
응용
심화
```

예시:

```markdown
1. [기초] 기본키와 외래키의 차이를 설명해 보세요.
2. [응용] students와 enrollments 테이블을 JOIN하여 학생별 수강 과목을 조회하는 SQL을 작성하세요.
3. [심화] AI가 생성한 아래 테이블 설계에서 정규화 관점의 문제를 찾아 수정하세요.
```

---

## 16. 프로젝트 Chapter 작성 규칙

### 16.1 Chapter 07 중간 프로젝트

Chapter 07은 중간 프로젝트 또는 중간 평가로 구성합니다.

목표:

```text
1~6장에서 배운 DBMS 기본 개념, SQL 기초, ERD, 정규화를 종합한다.
```

권장 주제:

```text
온라인 강의 수강신청 시스템
```

산출물:

```text
- 요구사항 정리
- ERD
- CREATE TABLE SQL
- 샘플 INSERT 데이터
- 기본 SELECT 쿼리
- AI 생성 결과 검토 보고서
```

### 16.2 Chapter 15 최종 프로젝트

Chapter 15는 최종 프로젝트 또는 최종 평가로 구성합니다.

목표:

```text
DB 설계부터 SQL 작성, 웹 CRUD 구현, GitHub 관리, AI 활용 검증까지 전체 흐름을 완성한다.
```

권장 주제:

```text
AI 튜터링 서비스 또는 RAG 문서 검색 시스템
```

산출물:

```text
- 요구사항 정의서
- ERD
- DDL
- 샘플 데이터
- 핵심 SQL
- 간단한 웹 CRUD 화면
- GitHub 저장소
- AI 활용 보고서
- 검증 보고서
```

---

## 17. Chapter 파일 작성 규칙

### 17.1 파일명 규칙

각 Chapter는 다음 파일명을 사용합니다.

```text
book/chapter01/chapter01_outline.md
book/chapter01/chapter01.md
book/chapter02/chapter02_outline.md
book/chapter02/chapter02.md
...
```

### 17.2 코드 경로 규칙

실습 코드는 다음 경로에 둡니다.

```text
code/chapter01/
code/chapter02/
...
```

### 17.3 이미지 경로 규칙

이미지는 다음 경로에 둡니다.

```text
images/chapter01/
images/chapter02/
...
```

---

## 18. 리뷰 기준

각 Chapter는 완료 전 다음 항목을 점검합니다.

```text
- 학습 목표가 명확한가?
- 초급자가 이해할 수 있는 문체인가?
- 핵심 개념 설명이 충분한가?
- 실습 코드가 실행 가능한가?
- SQL이 PostgreSQL 기준으로 작성되었는가?
- 도식 또는 표가 필요한 곳에 포함되었는가?
- AI 활용 부분이 단순 생성이 아니라 검증 중심인가?
- 자주 하는 실수와 해결 방법이 포함되었는가?
- 연습 문제가 포함되었는가?
- 다음 장과 자연스럽게 연결되는가?
```

---

## 19. 최종 원고 통합 기준

최종 원고는 다음 파일로 통합합니다.

```text
publish/full_manuscript.md
```

통합 전 확인 항목:

```text
- Chapter 번호와 제목 일관성
- 이미지 링크 정상 여부
- 코드 경로 정상 여부
- 표 번호와 그림 번호 정리
- 용어 표기 통일
- 실습 코드 저장소 경로 확인
- 부록과 참고자료 포함 여부
```

---

## 20. 전체 집필 원칙 요약

이 책의 모든 원고는 다음 원칙을 따릅니다.

```text
1. 어렵게 설명하지 않는다.
2. 개념은 예시와 함께 설명한다.
3. 실습은 작게 시작해서 점진적으로 확장한다.
4. AI가 만든 결과는 반드시 검증한다.
5. PostgreSQL 기준으로 통일한다.
6. DBeaver로 직접 확인하는 과정을 포함한다.
7. 웹 CRUD로 DB 설계 결과를 실행 가능한 형태로 검증한다.
8. Vector DB와 RAG는 초급자가 이해할 수 있는 수준에서 다룬다.
9. 각 Chapter는 하나의 수업 단위로 완결성을 가져야 한다.
10. 최종적으로 하나의 통합 eBook으로 출판 가능한 구조를 유지한다.
```
