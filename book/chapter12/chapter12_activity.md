# Chapter 12 독자 워크북

## 조회 패턴으로 RDBMS와 NoSQL 선택하기

> 이 워크북은 온라인 강의 서비스 데이터를 기준으로 원본·캐시·이벤트·관계 인덱스를 구분하고, RDBMS·PostgreSQL JSONB·NoSQL 유형 후보를 근거와 함께 비교하기 위한 자료입니다.

---

## 1. 실습 파일과 순서

```text
01_nosql_lab_schema.sql
02_nosql_lab_seed.sql
03_document_jsonb_queries.sql
04_key_value_cache_queries.sql
05_storage_choice_review.sql
reset_nosql_lab.sql
```

권장 실행 순서:

```text
01 → 02 → 03 → 04 → 05
```

이 실습은 별도 NoSQL 서버를 설치하지 않습니다. `nosql_lab`의 PostgreSQL JSONB와 일반 테이블로 저장 모델의 일부 개념만 검증합니다.

---

## 2. 보호 범위 확인

| 스키마 | Chapter 12 처리 | 확인 |
| --- | --- | --- |
| course_project | 변경 금지 |  |
| transaction_lab | 변경 금지 |  |
| performance_lab | 변경 금지 |  |
| security_lab | 변경 금지 |  |
| nosql_lab | 생성·실습·초기화 |  |

현재 데이터베이스:

```text
_______________________________________________________________
```

---

## 3. 시스템 역할 구분

| 데이터 | 시스템 역할 | 원본 위치 | 다시 만들 수 있는가 |
| --- | --- | --- | --- |
| 수강신청 | Source of Truth |  |  |
| 결제 상태 | Source of Truth |  |  |
| 로그인 세션 | Ephemeral State |  |  |
| 인기 강의 목록 | Derived Cache |  |  |
| 강의 옵션·태그 | Flexible Metadata |  |  |
| 학습 행동 | Event Log |  |  |
| 추천 관계 | Relationship Index |  |  |

원본과 파생 데이터를 구분해야 하는 이유:

```text
_______________________________________________________________
```

---

## 4. RDBMS와 NoSQL 비교

| 판단 항목 | RDBMS | NoSQL 후보 | 나의 기준 |
| --- | --- | --- | --- |
| 관계 무결성 |  |  |  |
| 다중 객체 트랜잭션 |  |  |  |
| 키 조회 |  |  |  |
| 문서 단위 조회 |  |  |  |
| 파티션 범위 조회 |  |  |  |
| 다단계 관계 탐색 |  |  |  |
| 구조 변경 |  |  |  |
| 운영·복구 |  |  |  |

NoSQL을 항상 더 빠르다고 말할 수 없는 이유:

```text
_______________________________________________________________
```

---

## 5. NoSQL 유형 구분

| 유형 | 핵심 구조 | 대표 조회 | 온라인 강의 사례 |
| --- | --- | --- | --- |
| Key-Value |  |  |  |
| Document |  |  |  |
| Column-Family |  |  |  |
| Graph |  |  |  |

다음 사례에 가장 먼저 검토할 유형을 작성합니다.

| 사례 | 후보 | 이유 |
| --- | --- | --- |
| 정확한 키로 로그인 세션 조회 |  |  |
| 강의 옵션과 태그를 문서 단위 조회 |  |  |
| 학생·날짜별 이벤트 시간순 조회 |  |  |
| 주제를 거쳐 추천 강의 관계 탐색 |  |  |

---

## 6. Key-Value 키·TTL 설계

| 항목 | 나의 설계 |
| --- | --- |
| 세션 키 |  |
| 인기 강의 키 |  |
| 키 버전 표현 |  |
| TTL |  |
| 캐시 미스 처리 |  |
| 원본 변경 시 무효화 |  |
| 캐시 장애 시 대응 |  |

키 이름에 네임스페이스와 버전을 포함하는 이유:

```text
_______________________________________________________________
```

캐시를 Source of Truth로 사용하면 안 되는 이유:

```text
_______________________________________________________________
```

---

## 7. Document 경계 판단

강의 상세 문서를 설계합니다.

| 속성 | 문서에 포함 | 별도 참조 | 이유 |
| --- | --- | --- | --- |
| course_code |  |  |  |
| title |  |  |  |
| tags |  |  |  |
| options |  |  |  |
| instructor 원본 |  |  |  |
| 수강신청 목록 |  |  |  |
| 누적 학습 이벤트 |  |  |  |

함께 읽는다는 이유만으로 계속 커지는 배열을 문서에 넣으면 안 되는 이유:

```text
_______________________________________________________________
```

유연한 스키마에도 필요한 규칙:

```text
필드 이름:
타입:
필수 필드:
문서 버전:
마이그레이션:
```

---

## 8. Column-Family 조회 패턴

목표 조회:

```text
학생 ______의 날짜 ______ 학습 이벤트를 시간순으로 읽는다.
```

| 설계 요소 | 값 | 이유 |
| --- | --- | --- |
| partition key |  |  |
| sort/clustering key |  |  |
| event_id |  |  |
| 보존 기간 |  |  |
| 파티션 크기 제한 |  |  |
| 늦게 도착한 이벤트 처리 |  |  |

새로운 조회가 추가될 때 별도 파생 테이블이 필요할 수 있는 이유:

```text
_______________________________________________________________
```

---

## 9. Graph DB와 JOIN 비교

| 질문 | RDBMS JOIN 우선 | Graph 검토 | 이유 |
| --- | --- | --- | --- |
| 강의별 수강 학생 |  |  |  |
| 학생별 수강 강의 |  |  |  |
| 학생→주제→강의 2단계 탐색 |  |  |  |
| 유사 학생 관계를 반복 확장 |  |  |  |
| 강의 간 선수 관계의 경로 탐색 |  |  |  |

관계가 존재하는 것과 관계 탐색이 핵심인 것의 차이:

```text
_______________________________________________________________
```

---

## 10. 트랜잭션·일관성 범위

| 질문 | 답변 |
| --- | --- |
| 한 키·문서 변경은 원자적인가 |  |
| 여러 문서·파티션 트랜잭션이 필요한가 |  |
| 쓰기 직후 최신 읽기가 필요한가 |  |
| 오래된 읽기를 허용할 수 있는가 |  |
| 네트워크 분할 시 원하는 동작은 무엇인가 |  |
| 충돌 해결 규칙은 무엇인가 |  |

“NoSQL은 모두 최종 일관성이다”라는 설명이 부정확한 이유:

```text
_______________________________________________________________
```

---

## 11. 여러 저장소 동기화

다음 실패 시나리오의 복구 방안을 작성합니다.

| 실패 | 남은 불일치 | 탐지 방법 | 복구 방법 |
| --- | --- | --- | --- |
| RDBMS COMMIT 후 캐시 삭제 실패 |  |  |  |
| 강의 수정 후 문서 복사본 갱신 실패 |  |  |  |
| 신청 성공 후 추천 그래프 갱신 실패 |  |  |  |
| 이벤트 중복 발행 |  |  |  |

검토할 패턴:

```text
원본 DB:
변경 이벤트:
재시도:
멱등성 키:
실패 대기열:
주기적 대조:
재구축:
```

---

## 12. nosql_lab 기준 데이터

| 테이블 | 기대 행 수 | 실제 행 수 |
| --- | ---: | ---: |
| nosql_lab.course_documents | 3 |  |
| nosql_lab.key_value_cache_examples | 4 |  |
| nosql_lab.storage_choice_cases | 6 |  |

기본 JSONB 조회:

| course_code | title | level | online |
| --- | --- | --- | --- |
| DB-101 | 데이터베이스 입문 | basic | true |
| AI-201 | AI 데이터 분석 | intermediate | true |
| GRAPH-301 | 그래프 데이터 이해 | advanced | false |

---

## 13. JSONB 연산자

| 연산자 | 나의 설명 | 실습 결과 |
| --- | --- | --- |
| `->` |  |  |
| `->>` |  |  |
| `?` |  |  |
| `@>` |  |  |

JSONB 문서의 핵심 속성을 일반 컬럼으로 분리한 이유:

```text
_______________________________________________________________
```

---

## 14. JSONB 문서 검증

| 검증 항목 | 기대 | 실제 |
| --- | --- | --- |
| metadata 객체 | 모두 true |  |
| level 키 | 모두 존재 |  |
| level 허용값 | 모두 정상 |  |
| tags 배열 | 모두 true |  |
| options.online boolean | 모두 true |  |
| document_version | 1 이상 |  |

문서 구조 검증을 DB CHECK 하나에 모두 넣지 않은 이유:

```text
_______________________________________________________________
```

---

## 15. JSONB 수정과 ROLLBACK

`DB-101`의 certificate 값을 트랜잭션 안에서 변경합니다.

| 확인 항목 | 변경 전 | 트랜잭션 내부 | ROLLBACK 후 |
| --- | --- | --- | --- |
| certificate | true | false | true |
| document_version | 1 | 2 | 1 |

기준 데이터를 유지하기 위해 ROLLBACK한 이유:

```text
_______________________________________________________________
```

---

## 16. JSONB 인덱스 후보

| 조건 | 후보 인덱스 | 이유 |
| --- | --- | --- |
| `metadata @> ...` |  |  |
| `metadata ->> 'level' = ...` |  |  |

표본 3행에서 `Seq Scan`이 나와도 오류가 아닌 이유:

```text
_______________________________________________________________
```

---

## 17. Key-Value 시뮬레이션

| 항목 | 기대 | 실제 |
| --- | ---: | ---: |
| 전체 행 | 4 |  |
| 유효 캐시 | 3 |  |
| 만료 캐시 | 1 |  |

이 테이블이 구현하지 않는 기능:

```text
메모리 저장:
자동 TTL 삭제:
eviction:
복제:
샤딩:
실제 장애 동작:
```

---

## 18. 저장 방식 선택표

| 데이터 | 역할 | 핵심 조회 | 후보 | 원본 | 판단 |
| --- | --- | --- | --- | --- | --- |
| 수강신청·결제 |  |  |  |  |  |
| 로그인 세션 |  |  |  |  |  |
| 인기 강의 |  |  |  |  |  |
| 강의 메타데이터 |  |  |  |  |  |
| 학습 이벤트 |  |  |  |  |  |
| 추천 관계 |  |  |  |  |  |

새 저장소가 필요하다는 증거:

```text
현재 한계:
대표 쿼리:
목표 수치:
실패 조건:
운영 담당:
```

---

## 19. 작은 PoC 계획

| 항목 | 계획 |
| --- | --- |
| 대표 조회 2~3개 |  |
| 데이터 크기·분포 |  |
| 읽기 목표 |  |
| 쓰기 목표 |  |
| 중복·재시도 시험 |  |
| 장애·복구 시험 |  |
| 백업·복원 시험 |  |
| 비용·운영 시간 |  |
| 기존 방식 비교 |  |

평균 응답 시간만으로 도입을 결정하면 안 되는 이유:

```text
_______________________________________________________________
```

---

## 20. AI 추천 검토

| AI 추천 | 원본 구분 | 조회 패턴 | 일관성 | 동기화 | 운영·복구 | 최종 판단 |
| --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |
|  |  |  |  |  |  |  |
|  |  |  |  |  |  |  |

대표 오류:

```text
모든 데이터를 NoSQL로 이동:
JSON이므로 무조건 Document DB:
로그이므로 무조건 Column-Family:
관계가 있으므로 무조건 Graph:
캐시 재구축 경로 없음:
이중 쓰기 실패 처리 없음:
```

---

## 21. 최종 점검

| 점검 항목 | 완료 |
| --- | --- |
| nosql_lab만 생성·초기화했다 |  |
| 앞 장 스키마를 변경하지 않았다 |  |
| 원본·캐시·이벤트·관계 인덱스를 구분했다 |  |
| NoSQL 네 유형의 목표 조회를 설명할 수 있다 |  |
| 제품별 트랜잭션·일관성 확인 필요성을 이해했다 |  |
| 여러 저장소의 동기화 실패를 검토했다 |  |
| JSONB와 전용 Document DB를 구분했다 |  |
| JSONB 핵심 컬럼·가변 메타데이터를 분리했다 |  |
| Key-Value 시뮬레이션의 한계를 설명할 수 있다 |  |
| 작은 PoC에 장애·복구 시험을 포함했다 |  |
| AI 추천을 운영 비용까지 검토했다 |  |

이 장의 핵심을 자신의 말로 작성합니다.

```text
올바른 저장소 선택은 ______________________________________________이다.
```
