# Chapter 03 독자 워크북

## PostgreSQL과 DBeaver로 실습 환경 만들기

> 이 워크북은 Chapter 03을 읽은 뒤 자신의 PostgreSQL 환경을 점검하고 실행 결과를 기록하기 위한 선택형 보조 자료입니다. 테이블 생성이나 데이터 입력보다 연결 대상, 데이터베이스, 스키마와 SQL 실행 상태를 정확히 확인하는 데 집중합니다.

---

## 1. 워크북 활용 방법

이 장의 핵심 흐름은 다음과 같습니다.

```text
PostgreSQL 서버 준비
→ DBeaver 연결
→ ai_database_book 생성
→ 새 데이터베이스로 다시 연결
→ public 스키마 확인
→ 환경 검증 SQL 실행
→ setup_check.sql 저장
```

다음 질문에 자신의 환경을 기준으로 답해 봅니다.

```text
PostgreSQL 서버는 실제로 실행 중인가?
DBeaver는 어느 데이터베이스에 연결되어 있는가?
현재 스키마는 무엇인가?
SQL 한 문장과 전체 스크립트 실행을 구분할 수 있는가?
같은 환경 확인 파일을 여러 번 안전하게 실행할 수 있는가?
비밀번호와 접속 정보가 공개 파일에서 분리되어 있는가?
```

비밀번호, 전체 접속 URL, 실제 `.env` 값, API Key와 Access Token은 이 워크북이나 공개 저장소에 기록하지 않습니다.

---

## 2. 현재 실습 환경 기록하기

| 항목 | 나의 환경 |
| --- | --- |
| 운영체제 |  |
| PostgreSQL 사용 방식 | 로컬 / 클라우드 |
| PostgreSQL 버전 |  |
| DBeaver 버전 |  |
| Host |  |
| Port |  |
| 처음 연결한 데이터베이스 |  |
| 작업용 데이터베이스 |  |
| 현재 스키마 |  |
| SQL 파일 보관 위치 |  |

### 선택 이유

```text
나는 [로컬 / 클라우드] PostgreSQL을 선택했다.
그 이유는 __________________________________________________ 때문이다.
```

---

## 3. PostgreSQL과 DBeaver 역할 구분하기

다음 작업을 담당하는 구성 요소를 적어 보세요.

| 작업 | PostgreSQL / DBeaver / 사용자 | 이유 |
| --- | --- | --- |
| SQL을 작성한다 |  |  |
| 데이터를 실제로 저장한다 |  |  |
| 사용자 인증을 확인한다 |  |  |
| SQL을 실행한다 |  |  |
| 실행 결과를 화면에 표시한다 |  |  |
| 데이터베이스와 스키마 목록을 탐색한다 |  |  |
| 비밀번호를 안전하게 관리한다 |  |  |

다음 문장을 완성합니다.

```text
PostgreSQL은 ____________________________________________ 이고,
DBeaver는 ______________________________________________ 이다.
```

```text
DBeaver를 종료해도 데이터가 유지되는 이유는
________________________________________________________________________ 때문이다.
```

---

## 4. 연결 정보 점검하기

| 항목 | 값 | 의미 |
| --- | --- | --- |
| DB 종류 | PostgreSQL | 연결할 DBMS 종류 |
| Host |  | 서버가 실행되는 컴퓨터 주소 |
| Port |  | 서버 접속 통로 |
| Database |  | 처음 연결할 데이터베이스 |
| Username |  | 접속 사용자 |
| Password | 기록하지 않음 | 사용자 인증 정보 |
| Test Connection 결과 |  | 연결 성공 여부 |

### 개념 구분

```text
postgres 사용자:
________________________________________________________________________

postgres 데이터베이스:
________________________________________________________________________
```

### 연결 실패 기록

| 항목 | 기록 |
| --- | --- |
| 오류 메시지 |  |
| 서버 상태 확인 결과 |  |
| Host와 Port 확인 결과 |  |
| Username 확인 결과 |  |
| 적용한 해결 방법 |  |

---

## 5. 작업용 데이터베이스 만들기

기본 `postgres` 데이터베이스에 연결한 SQL 편집기에서 다음 문장을 단독으로 실행합니다.

```sql
CREATE DATABASE ai_database_book;
```

| 확인 항목 | 기록 |
| --- | --- |
| 생성 성공 여부 |  |
| 이미 존재한다는 메시지 여부 |  |
| DBeaver 목록에서 확인 여부 |  |
| 새 연결 또는 연결 대상 변경 여부 |  |

다음 메시지가 표시됐다면 의미를 적어 보세요.

```text
database "ai_database_book" already exists
```

```text
의미:
________________________________________________________________________

추가로 해야 할 작업:
________________________________________________________________________
```

---

## 6. 현재 데이터베이스와 스키마 확인하기

새 `ai_database_book` 연결의 SQL 편집기에서 다음 SQL을 실행합니다.

```sql
SELECT current_database();

SELECT current_schema();

SHOW search_path;
```

| SQL | 기대 결과 | 실제 결과 |
| --- | --- | --- |
| `current_database()` | `ai_database_book` |  |
| `current_schema()` | `public` |  |
| `SHOW search_path` | `"$user", public`과 유사한 값 |  |

### 확인 질문

```text
데이터베이스를 생성한 뒤 기존 SQL 편집기의 연결 대상이 자동으로 바뀌었는가?
________________________________________________________________________

현재 데이터베이스와 현재 스키마를 각각 확인해야 하는 이유는 무엇인가?
________________________________________________________________________
```

---

## 7. DBeaver에서 구조 찾아보기

DBeaver의 Database Navigator에서 다음 위치를 찾아봅니다.

```text
PostgreSQL 연결
→ ai_database_book
→ Schemas
→ public
→ Tables
```

| 확인 항목 | 결과 |
| --- | --- |
| `ai_database_book` 연결을 찾았는가? |  |
| `Schemas` 항목을 찾았는가? |  |
| `public` 스키마를 찾았는가? |  |
| `Tables` 항목을 찾았는가? |  |
| 현재 Tables가 비어 있는가? |  |

Chapter 04 전에는 `Tables` 아래가 비어 있어도 정상입니다.

테이블이 보이지 않을 때 확인할 항목을 적어 보세요.

```text
1. ______________________________________________________________________
2. ______________________________________________________________________
3. ______________________________________________________________________
```

---

## 8. SQL 편집기 실행 범위 구분하기

다음 SQL을 편집기에 작성합니다.

```sql
SELECT current_database();
SELECT current_schema();
SELECT current_user;
```

각 실행 방법의 결과를 기록합니다.

| 실행 방식 | 내가 선택한 범위 | 실행된 문장 수 | 결과 |
| --- | --- | ---: | --- |
| 현재 문장 실행 |  |  |  |
| 선택 영역 실행 |  |  |  |
| 전체 스크립트 실행 |  |  |  |

### 확인 질문

```text
세미콜론은 어떤 역할을 하는가?
________________________________________________________________________

여러 변경 SQL이 들어 있는 파일을 전체 실행하기 전에 무엇을 확인해야 하는가?
________________________________________________________________________
```

---

## 9. 환경 검증 SQL 실행하기

다음 SQL을 실행합니다.

```sql
SELECT version();

SELECT current_database();

SELECT current_schema();

SELECT current_user;

SELECT CURRENT_TIMESTAMP AS checked_at;

SELECT 1 + 1 AS result;
```

| 확인 항목 | 기대 내용 | 실제 결과 |
| --- | --- | --- |
| PostgreSQL 버전 | 버전 문자열 |  |
| 현재 데이터베이스 | `ai_database_book` |  |
| 현재 스키마 | `public` |  |
| 현재 사용자 | 접속 계정 |  |
| 서버 시각 | 날짜와 시각 |  |
| 계산 결과 | `2` |  |

### 결과 해석

```text
모든 결과가 예상과 일치하는가?
________________________________________________________________________

일치하지 않은 항목이 있다면 원인은 무엇인가?
________________________________________________________________________
```

---

## 10. `setup_check.sql` 파일로 남기기

환경 확인 SQL을 다음 파일에 저장합니다.

```text
code/chapter03/setup_check.sql
```

파일에는 다음 조회문만 포함합니다.

```text
version()
current_database()
current_schema()
current_user
CURRENT_TIMESTAMP
1 + 1
```

| 파일 관리 항목 | 확인 |
| --- | --- |
| 환경 확인 SQL만 포함했는가? |  |
| 테이블 생성 SQL이 없는가? |  |
| 데이터 입력·수정·삭제 SQL이 없는가? |  |
| 각 SQL의 목적을 주석으로 설명했는가? |  |
| 여러 번 실행해도 데이터가 바뀌지 않는가? |  |
| 비밀번호와 접속 URL이 없는가? |  |

### 재실행 기록

| 실행 시점 | 현재 DB | 현재 스키마 | 결과 | 비고 |
| --- | --- | --- | --- | --- |
| 첫 실행 |  |  |  |  |
| DBeaver 재실행 후 |  |  |  |  |
| 오류 해결 후 |  |  |  |  |

---

## 11. 로컬과 클라우드 환경 판단하기

| 판단 기준 | 로컬 PostgreSQL | 관리형 PostgreSQL |
| --- | --- | --- |
| 프로그램 설치 필요 |  |  |
| 인터넷 연결 필요 |  |  |
| 데이터 위치 |  |  |
| 다른 기기 접근 |  |  |
| 접속 정보 관리 |  |  |
| 서버 구조 이해 |  |  |
| 나에게 적합한 상황 |  |  |

다음 문장을 완성합니다.

```text
현재 나에게 더 적합한 환경은 __________________________ 이다.
가장 중요한 판단 근거는 ______________________________________________ 이다.
```

---

## 12. 비밀정보 점검하기

다음 항목을 공개 파일에 포함해도 되는지 판단해 보세요.

| 항목 | 공개 가능 / 금지 | 이유 |
| --- | --- | --- |
| `DB_PORT=5432` |  |  |
| 실제 DB 비밀번호 |  |  |
| 전체 클라우드 접속 URL |  |  |
| `.env.example`의 변수 이름 |  |  |
| 실제 값이 들어 있는 `.env` |  |  |
| 오류 메시지의 일반 문구 |  |  |
| 실제 서버 주소가 포함된 화면 캡처 |  |  |

다음 세 규칙을 자신의 언어로 다시 적어 보세요.

```text
1. ______________________________________________________________________
2. ______________________________________________________________________
3. ______________________________________________________________________
```

---

## 13. 오류를 유형별로 분류하기

| 오류 또는 증상 | 서버 / 인증 / DB / 스키마 / SQL | 우선 확인할 내용 |
| --- | --- | --- |
| Connection refused |  |  |
| Password authentication failed |  |  |
| Database does not exist |  |  |
| `psql` not recognized |  |  |
| 테이블이 보이지 않음 |  |  |
| SQL syntax error |  |  |

### 문제 해결 기록

```markdown
## 발생한 문제

## 실행하려던 작업

## 오류 메시지

## 실행 환경

## 오류 유형

## 확인한 순서

## 해결 방법

## 해결 후 다시 실행한 결과
```

---

## 14. AI 질문을 재현 가능한 형태로 작성하기

다음 문장은 정보가 부족합니다.

```text
DBeaver가 안 됩니다. 해결해 주세요.
```

아래 양식으로 바꾸어 작성합니다.

```text
운영체제:
PostgreSQL 사용 방식: 로컬 / 클라우드
PostgreSQL 버전:
DBeaver 버전:

연결값:
- Host:
- Port:
- Database:
- Username:

실행한 작업:
발생한 오류 메시지:
이미 확인한 내용:

비밀번호와 전체 접속 URL은 제외했습니다.
가능한 원인을 우선순위대로 설명하고,
각 원인을 안전하게 확인하는 방법을 알려 주세요.
```

### AI 답변 검토표

| 검토 항목 | 결과 |
| --- | --- |
| 현재 운영체제와 환경에 맞는가? |  |
| 삭제나 초기화 명령이 포함되어 있는가? |  |
| 각 명령의 목적을 이해할 수 있는가? |  |
| 한 번에 하나씩 검증할 수 있는가? |  |
| 비밀정보 입력을 요구하지 않는가? |  |
| 실제 해결 여부를 다시 실행해 확인했는가? |  |

---

## 15. 최종 완료 점검

| 점검 항목 | 완료 |
| --- | --- |
| PostgreSQL 서버 또는 관리형 DB를 사용할 수 있다. |  |
| DBeaver Test Connection에 성공했다. |  |
| `ai_database_book` 데이터베이스를 만들고 연결했다. |  |
| `current_database()` 결과를 확인했다. |  |
| `current_schema()` 결과를 확인했다. |  |
| `public` 스키마의 `Tables` 위치를 찾았다. |  |
| SQL 한 문장, 선택 영역, 전체 스크립트 실행을 구분했다. |  |
| 환경 검증 SQL 여섯 개를 실행했다. |  |
| `setup_check.sql`을 저장하고 다시 실행했다. |  |
| SQL과 문서에 비밀정보가 없는지 확인했다. |  |
| 오류 발생 시 원인과 해결 과정을 기록했다. |  |

### 한 문장 정리

```text
Chapter 03 실습 환경이 준비되었다고 판단할 수 있는 근거는
________________________________________________________________________ 이다.
```

---

## 16. 다음 장 준비

Chapter 04에서는 다음 작업을 별도 SQL 파일에서 시작합니다.

```text
CREATE TABLE
INSERT
SELECT
WHERE
ORDER BY
UPDATE
DELETE
```

Chapter 03의 `setup_check.sql`은 환경 확인 전용 파일로 유지합니다. 테이블 생성, 데이터 입력과 제약조건 오류 실습은 이 파일에 추가하지 않습니다.
