(() => {
  'use strict';

  const slides = window.CHAPTER_DATA?.slides || [];
  const byId = new Map(slides.map((slide) => [slide.id, slide]));
  const step = (target, label, script, pointerNote = '') => ({ target, label, script, pointerNote });
  const card = (cue, title, text, cls = '') => `<article class="card cue-target ${cls}" data-cue="${cue}"><h3>${title}</h3><p>${text}</p></article>`;
  const numberedCard = (cue, number, title, text, cls = '') => `<article class="card cue-target ${cls}" data-cue="${cue}" style="position:relative;padding-top:36px"><span style="position:absolute;top:12px;left:50%;transform:translateX(-50%);display:inline-flex;align-items:center;justify-content:center;width:28px;height:28px;border-radius:999px;background:#155eef;color:#fff;font-weight:900">${number}</span><h3>${title}</h3><p>${text}</p></article>`;
  const apply = (id, patch) => {
    const slide = byId.get(id);
    if (!slide) throw new Error(`Chapter 02 semantic override target not found: ${id}`);
    Object.assign(slide, patch);
  };

  // 11페이지: 한 장의 SVG 전체가 아니라 요청의 왕복 흐름을 다섯 의미 단계로 이동합니다.
  apply('part03-01', {
    body: `<div class="flow">
      <div class="flow-step cue-target" data-cue="user">사용자<br><small>질문과 SQL 작성</small></div><div class="flow-arrow">→</div>
      <div class="flow-step cue-target" data-cue="client">클라이언트<br><small>DBeaver</small></div><div class="flow-arrow">→</div>
      <div class="flow-step cue-target" data-cue="dbms">PostgreSQL<br><small>DBMS 실행</small></div><div class="flow-arrow">→</div>
      <div class="flow-step cue-target" data-cue="result">결과·오류<br><small>화면에 반환</small></div><div class="flow-arrow">→</div>
      <div class="flow-step cue-target" data-cue="human">사람의 판단<br><small>질문과 결과 검증</small></div>
    </div>`,
    script: '사용자의 요청은 클라이언트와 디비엠에스를 거쳐 결과로 돌아오며 마지막에는 사람이 그 결과의 의미를 검증합니다.',
    steps: [
      step('user','1. 질문과 SQL 작성','먼저 사용자가 해결하려는 질문을 정하고 에스큐엘을 작성합니다. 어떤 결과가 필요한지 사람이 먼저 정의해야 명령의 목적도 분명해집니다. 출발점은 도구가 아니라 질문입니다.'),
      step('client','2. 클라이언트가 전달','디비버 같은 클라이언트는 사용자가 작성한 에스큐엘을 포스트그레스큐엘에 전달합니다. 클라이언트는 명령을 보내고 결과를 보여 주는 도구이지 데이터를 직접 보관하는 서버가 아닙니다. 따라서 화면 프로그램과 저장 주체의 역할을 구분합니다.'),
      step('dbms','3. DBMS가 실행','포스트그레스큐엘 디비엠에스가 전달받은 명령을 실제로 실행합니다. 데이터와 제약조건을 관리하고 요청에 맞는 결과 또는 오류를 만들어 냅니다. 이 단계가 실제 데이터 처리의 중심입니다.'),
      step('result','4. 결과가 돌아옴','실행 결과나 오류는 다시 클라이언트로 돌아와 화면에 표시됩니다. 화면에 표가 보인다는 사실은 요청이 처리되었다는 뜻일 뿐 결과의 업무 의미가 맞다는 뜻은 아닙니다. 반환된 내용은 다음 검증 단계의 입력입니다.'),
      step('human','5. 사람이 검증','마지막으로 사람은 처음 질문과 화면의 결과가 실제로 일치하는지 확인합니다. 실행 성공과 올바른 답을 같은 것으로 보지 않는 것이 중요합니다. 도구는 결과를 보여 주지만 판단 책임까지 대신하지는 않습니다.')
    ]
  });

  // 15페이지: PostgreSQL 계층을 다섯 단계로 분리합니다.
  apply('part04-01', {
    body: `<div class="flow">
      <div class="flow-step cue-target" data-cue="server">PostgreSQL 서버</div><div class="flow-arrow">→</div>
      <div class="flow-step cue-target" data-cue="database">데이터베이스</div><div class="flow-arrow">→</div>
      <div class="flow-step cue-target" data-cue="schema">스키마</div><div class="flow-arrow">→</div>
      <div class="flow-step cue-target" data-cue="objects">객체<br><small>테이블·뷰·시퀀스·함수</small></div><div class="flow-arrow">→</div>
      <div class="flow-step cue-target" data-cue="table">테이블<br><small>행과 열</small></div>
    </div><div class="quote" style="margin-top:18px;font-size:26px">오류를 해석할 때는 현재 어느 서버 · 데이터베이스 · 스키마의 어떤 객체를 보고 있는지 확인합니다.</div>`,
    script: '포스트그레스큐엘의 저장 구조는 서버에서 데이터베이스와 스키마, 객체, 테이블의 행과 열로 이어지는 계층으로 읽습니다.',
    steps: [
      step('server','1. PostgreSQL 서버','가장 바깥에는 포스트그레스큐엘 서버가 있습니다. 하나의 서버는 여러 데이터베이스를 관리할 수 있습니다. 연결 문제를 볼 때 먼저 어느 서버에 접속했는지 확인합니다.'),
      step('database','2. 데이터베이스','서버 안에는 여러 데이터베이스가 존재할 수 있습니다. 데이터베이스는 관련 데이터를 담는 논리적인 작업 공간이며 디비엠에스 소프트웨어 자체와는 다릅니다. 이번 책에서는 에이아이 데이터베이스 북 데이터베이스를 사용합니다.'),
      step('schema','3. 스키마','각 데이터베이스 안에는 스키마가 있습니다. 스키마는 테이블과 같은 객체를 묶는 이름 공간이므로 데이터베이스 자체와 구분해서 읽어야 합니다. 같은 테이블 이름도 서로 다른 스키마에 존재할 수 있습니다.'),
      step('objects','4. 데이터베이스 객체','스키마 안에는 테이블뿐 아니라 뷰와 시퀀스, 함수 같은 여러 객체가 들어갈 수 있습니다. 이번 장에서는 존재 관계를 확인하되 테이블을 중심으로 학습합니다. 객체 종류마다 역할이 다르다는 점만 먼저 구분합니다.'),
      step('table','5. 테이블과 행·열','테이블은 행과 열로 데이터를 저장하는 핵심 객체입니다. 테이블을 볼 때는 열 이름보다 먼저 한 행이 무엇을 나타내는지 확인합니다. 현재 위치와 한 행의 의미를 함께 알아야 조회 결과도 정확하게 해석할 수 있습니다.')
    ]
  });

  // 19페이지: 테이블·행·열·셀을 각각 독립 단계로 이동합니다.
  apply('part05-01', {
    body: `<div class="grid-4">
      ${numberedCard('table','1','테이블 전체','students 표 전체가 하나의 테이블입니다.')}
      ${numberedCard('row','2','한 행','가로 한 줄은 학생 한 명의 기록입니다.')}
      ${numberedCard('column','3','한 열','세로 항목은 id · name · email · major입니다.')}
      ${numberedCard('cell','4','한 셀','특정 행과 열이 만나는 실제 값 하나입니다.','good')}
    </div><div class="quote" style="margin-top:18px;font-size:26px">표를 볼 때 범위를 구분한 뒤, 한 행의 의미를 먼저 설명합니다.</div>`,
    script: '표 안의 테이블, 행, 열, 셀은 서로 다른 범위를 가리키므로 네 개념을 화면에서도 각각 분리해서 확인합니다.',
    steps: [
      step('table','1. 테이블 전체','스튜던츠 표 전체가 하나의 테이블입니다. 테이블은 같은 성격의 기록을 행과 열의 구조로 저장합니다. 먼저 어디까지가 한 테이블인지 범위를 확인합니다.'),
      step('row','2. 한 행','가로 한 줄은 하나의 행입니다. 스튜던츠 테이블에서는 한 행이 학생 한 명의 기록을 나타냅니다. 이후 조회와 집계를 해석할 때도 이 한 행의 의미가 기준이 됩니다.'),
      step('column','3. 한 열','세로 방향의 아이디, 네임, 이메일, 메이저는 각각 열입니다. 같은 열에는 모든 행에서 같은 성격의 값이 들어갑니다. 열은 기록을 구성하는 공통 항목이라고 이해합니다.'),
      step('cell','4. 한 셀','특정 행과 특정 열이 만나는 한 값이 셀입니다. 예를 들어 김민지 학생 행과 메이저 열이 만나는 값이 하나의 셀입니다. 셀의 위치보다 어떤 행의 어떤 열인지 의미로 읽는 습관이 중요합니다.')
    ]
  });

  // 23페이지: 저장 구조와 조회 결과를 두 단계로 분리합니다.
  apply('part06-01', {
    body: `<div class="grid-2">
      ${card('stored','원본 테이블','데이터를 실제로 저장하는 구조<br>예: students의 전체 행과 열')}
      ${card('result','조회 결과','SQL이 선택·조건·정렬·계산하여 만든 결과 집합','good')}
    </div><div class="quote" style="margin-top:18px;font-size:27px">둘 다 표처럼 보여도 같은 개념이 아닙니다.</div>`,
    script: '테이블은 데이터를 저장하는 구조이고 조회 결과는 질문에 따라 만들어지는 결과 집합이므로 두 표를 같은 것으로 보지 않습니다.',
    steps: [
      step('stored','1. 원본 저장 구조','테이블은 데이터를 실제로 저장하는 구조입니다. 스튜던츠 테이블에는 정의된 열과 저장된 학생 행이 유지됩니다. 화면에 일부만 보인다고 원본 구조가 그만큼만 존재하는 것은 아닙니다.'),
      step('result','2. 조회 결과','조회 결과는 에스큐엘이 선택한 열과 조건, 정렬, 계산에 따라 그때 만들어진 결과 집합입니다. 원본의 일부만 보이거나 여러 테이블을 연결한 새로운 모양이 나타날 수도 있습니다. 따라서 디비버의 결과 표를 원본 테이블 전체라고 단정하지 않습니다.')
    ]
  });

  // 26페이지: 활동 장표는 전체 질문 → 정답 공개 한 번으로 고정합니다.
  apply('part06-04', {
    script: '현재 보이는 표가 원본 전체인지 판단하려면 화면 모양이 아니라 실행한 에스큐엘과 적용된 변형을 확인합니다.',
    steps: [
      step('answer','정답 공개','먼저 선택한 열과 필터 조건을 확인합니다. 연결이나 계산과 집계가 적용되었다면 결과의 한 행 의미도 다시 정의해야 합니다. 또한 오더 바이가 없다면 첫 번째 행을 고정된 대상이라고 생각하지 않습니다.')
    ]
  });

  // 28페이지: 두 식별자 이후 별도 가상 단계가 생기지 않도록 두 단계 안에서 정리합니다.
  apply('part07-02', {
    script: '아이디와 학번은 모두 식별에 쓰일 수 있지만 데이터베이스 내부 식별자와 업무 식별자는 목적이 다릅니다.',
    steps: [
      step('internal','1. 내부 식별자','아이디 1과 같은 내부 식별자는 데이터베이스 안에서 특정 행을 안정적으로 구분하는 기준입니다. 다른 테이블이 이 값을 참조하면 이름이 바뀌어도 같은 대상을 안정적으로 연결할 수 있습니다. 사용자에게 의미 있는 번호라기보다 구조를 위한 식별 기준에 가깝습니다.'),
      step('business','2. 업무 식별자','스튜던트 넘버는 학교 업무에서 학생을 구분하고 표시하기 위한 학번입니다. 정책과 형식이 바뀔 수 있고 앞자리 영이 의미를 가질 수도 있습니다. 숫자처럼 보여도 계산용 숫자인지 먼저 업무 목적을 확인해야 합니다.')
    ]
  });

  // 29페이지: 활동 장표는 정답 공개 한 단계만 사용합니다.
  apply('part07-03', {
    script: '숫자처럼 보이는 식별자는 모양이 아니라 계산 여부와 앞자리 보존 같은 업무 의미를 기준으로 타입을 판단합니다.',
    steps: [
      step('answer','정답 공개','학번은 서로 더하거나 평균을 계산하기보다 학생을 구분하고 표시하는 데 사용합니다. 회원번호의 앞자리 영이 번호 체계의 일부라면 그 영도 그대로 보존해야 합니다. 따라서 계산용이 아닌 식별 값은 문자열 방식이 자연스러운지 검토하고 구체 타입은 4장에서 결정합니다.')
    ]
  });

  // 30페이지: 기본키 화면을 예시 → 식별 → 규칙의 세 단계로 분리합니다.
  apply('part08-01', {
    body: `<div class="grid-3">
      ${card('same-name','같은 이름','김민지<br>김민지<br><small>이름만으로는 구분 어려움</small>','warn')}
      ${card('identity','기본키로 구분','id = 1<br>id = 2<br><small>서로 다른 학생 행</small>','good')}
      ${card('rules','기본키 규칙','중복되지 않음<br>NULL일 수 없음')}
    </div>`,
    script: '기본키는 이름처럼 중복될 수 있는 값 대신 자신의 테이블에서 각 행을 안정적으로 식별하는 기준입니다.',
    steps: [
      step('same-name','1. 같은 이름은 식별자가 아님','같은 이름의 김민지 학생이 두 명 있을 수 있습니다. 이름은 중복될 수 있고 나중에 변경될 수도 있으므로 이름만으로 행을 안정적으로 구분하기 어렵습니다. 먼저 사람이 읽는 값과 식별 기준을 분리해서 생각합니다.'),
      step('identity','2. 기본키로 행 구분','한 학생의 아이디가 1이고 다른 학생의 아이디가 2라면 서로 다른 두 행을 명확하게 구분할 수 있습니다. 기본키의 핵심 역할은 자신의 테이블에서 한 행을 안정적으로 식별하는 것입니다. 다른 테이블이 이 키를 참조하면 관계도 명확해집니다.'),
      step('rules','3. 중복과 NULL 금지','기본키 값은 서로 다른 두 행에서 중복될 수 없습니다. 또한 행을 구분해야 하는 값이 비어 있으면 식별 기준이 될 수 없으므로 널도 허용하지 않습니다. 복합 기본키와 세부 설계는 후속 장에서 다룹니다.')
    ]
  });

  // 31페이지: 두 참조와 외래키 반복을 각각 분리합니다.
  apply('part08-02', {
    body: `<div class="grid-3">
      ${card('student-fk','student_id → students.id','수강신청이 어떤 학생의 것인지 참조')}
      ${card('course-fk','course_id → courses.id','수강신청이 어떤 강의인지 참조')}
      ${card('repeat-fk','FK는 반복 가능','한 학생의 여러 신청에서 같은 student_id가 반복될 수 있음','good')}
    </div>`,
    script: '외래키는 다른 테이블의 행을 참조해서 업무 관계를 표현하며 일대다 관계에서는 같은 외래키 값이 자연스럽게 반복될 수 있습니다.',
    steps: [
      step('student-fk','1. 학생을 참조','인롤먼츠의 스튜던트 아이디는 스튜던츠의 아이디를 참조합니다. 이 값으로 해당 신청이 어느 학생에게 속하는지 연결할 수 있습니다. 화살표의 방향은 수강신청의 외래키에서 학생 테이블의 식별 키로 향합니다.'),
      step('course-fk','2. 강의를 참조','인롤먼츠의 코스 아이디는 코시즈의 아이디를 참조합니다. 한 수강신청 행은 학생과 강의라는 두 대상을 각각 키로 연결합니다. 이름이나 제목을 반복해서 저장하는 것보다 참조 관계를 명확하게 표현할 수 있습니다.'),
      step('repeat-fk','3. 외래키는 반복 가능','학생 한 명이 여러 강의를 신청하면 여러 인롤먼츠 행에서 같은 스튜던트 아이디가 반복될 수 있습니다. 이것은 중복 오류가 아니라 여러 자식 행이 같은 부모 행을 참조하는 일대다 관계의 정상적인 표현입니다. 외래키는 자동으로 유니크가 되는 값이 아닙니다.')
    ]
  });

  // 33페이지: 활동 장표는 정답 공개 한 단계만 사용합니다.
  apply('part08-04', {
    script: '같은 이름의 학생을 구분하는 기본키와 여러 신청에서 반복될 수 있는 외래키의 역할을 한 번에 확인합니다.',
    steps: [
      step('answer','정답 공개','이름이 같은 두 학생은 스튜던츠 아이디 기본키로 서로 다른 행을 구분합니다. 인롤먼츠의 스튜던트 아이디는 그 학생을 참조하는 외래키이며 한 학생이 여러 강의를 신청하면 같은 값이 여러 신청 행에 나타날 수 있습니다. 기본키는 구분, 외래키는 참조라는 역할을 분리해서 이해합니다.')
    ]
  });

  // 34페이지: 카디널리티 세 유형을 각각 이동합니다.
  apply('part09-01', {
    body: `<div class="grid-3">
      ${numberedCard('one-one','1','1 : 1','사용자 한 명 ↔ 프로필 한 개')}
      ${numberedCard('one-many','2','1 : N','학생 한 명 → 여러 질문')}
      ${numberedCard('many-many','3','N : M','여러 학생 ↔ 여러 강의','good')}
    </div><div class="quote" style="margin-top:18px;font-size:26px">카디널리티는 한쪽 대상이 상대편과 최대 몇 개까지 연결될 수 있는지를 나타냅니다.</div>`,
    script: '카디널리티는 업무 대상 사이의 최대 연결 개수를 일대일, 일대다, 다대다로 구분해서 표현합니다.',
    steps: [
      step('one-one','1. 일대일','일대일은 한쪽의 한 대상이 상대편의 최대 한 대상과 연결되는 관계입니다. 사용자 한 명과 상세 프로필 한 개를 예로 생각할 수 있습니다. 양쪽 모두 최대 하나씩 연결된다는 점을 확인합니다.'),
      step('one-many','2. 일대다','일대다는 한쪽의 한 대상이 상대편의 여러 대상과 연결될 수 있는 관계입니다. 학생 한 명이 여러 질문을 작성하는 상황이 대표적인 예입니다. 데이터베이스에서는 여러 자식 행이 같은 부모 키를 참조하는 모습으로 자주 나타납니다.'),
      step('many-many','3. 다대다','다대다는 양쪽 모두 여러 대상을 서로 연결할 수 있는 관계입니다. 학생은 여러 강의를 신청할 수 있고 하나의 강의에도 여러 학생이 등록할 수 있습니다. 관계형 데이터베이스에서는 이런 관계를 보통 연결 테이블로 풀어서 표현합니다.')
    ]
  });

  // 35페이지: 이미 세 단계인 연결 테이블 흐름 안에서 결론까지 마칩니다.
  apply('part09-02', {
    script: '학생과 강의의 다대다 관계는 인롤먼츠 연결 테이블을 사이에 두어 두 개의 일대다 관계로 표현합니다.',
    steps: [
      step('students','1. 학생 1 대 신청 N','학생 한 명은 여러 수강신청 행과 연결될 수 있습니다. 따라서 스튜던츠 한 행에서 인롤먼츠 여러 행으로 일대다 관계가 만들어집니다. 학생 정보 자체와 신청 사건을 서로 다른 테이블로 구분합니다.'),
      step('bridge','2. 연결 자체를 기록','인롤먼츠의 한 행은 학생 한 명이 강의 한 개를 신청한 사건 한 건입니다. 신청일이나 신청 당시 금액처럼 관계에서 발생한 정보도 이 연결 테이블에 둘 수 있습니다. 단순한 연결선이 아니라 업무 사건을 저장하는 테이블입니다.'),
      step('courses','3. 강의 1 대 신청 N','강의 한 개에도 여러 학생의 수강신청 행이 연결될 수 있습니다. 그래서 코시즈와 인롤먼츠 사이에도 일대다 관계가 만들어집니다. 결과적으로 학생과 강의의 다대다 관계가 두 개의 일대다 관계로 분해됩니다.')
    ]
  });

  // 36페이지: 두 용어를 두 단계 안에서 마칩니다.
  apply('part09-03', {
    script: '릴레이션과 릴레이션십은 모두 관계로 번역되지만 데이터 구조와 업무 대상 사이의 연결이라는 서로 다른 개념입니다.',
    steps: [
      step('relation','1. Relation','릴레이션은 관계 모델에서 행과 열로 구성된 구조를 가리키는 이론적인 용어입니다. 화면에서 보는 테이블과 비슷한 형태를 떠올리면 이해하기 쉽습니다. 지금은 수학적인 정의보다 구조 용어라는 점만 구분합니다.'),
      step('relationship','2. Relationship','릴레이션십은 학생이 강의를 신청한다처럼 현실의 업무 대상들이 서로 어떻게 연결되는지를 뜻합니다. 일대일, 일대다, 다대다와 연결 테이블을 설명할 때 사용하는 관계가 여기에 해당합니다. 지금의 핵심은 용어 암기보다 카디널리티와 연결 구조를 읽는 것입니다.')
    ]
  });

  // 38페이지: CRUD 네 단계 안에서 SQL과의 관계까지 각각 설명합니다.
  apply('part10-02', {
    script: '크러드는 서비스에서 반복되는 생성, 조회, 수정, 삭제의 네 가지 기본 데이터 작업이며 에스큐엘은 이를 관계형 디비엠에스에 전달하는 언어입니다.',
    steps: [
      step('create','1. Create','크리에이트는 새로운 데이터를 생성하는 작업입니다. 새 학생을 등록하거나 새로운 수강신청 행을 추가하는 기능이 여기에 해당합니다. 실제 입력 에스큐엘은 4장에서 실행합니다.'),
      step('read','2. Read','리드는 이미 저장된 데이터에서 필요한 내용을 조회하는 작업입니다. 학생 정보나 강의 목록을 확인하는 서비스 기능과 연결됩니다. 조회 결과는 원본 테이블 전체와 다를 수 있다는 점도 함께 기억합니다.'),
      step('update','3. Update','업데이트는 이미 존재하는 데이터의 값을 수정하는 작업입니다. 학생의 이메일이나 전공 정보가 변경되는 상황을 예로 들 수 있습니다. 어떤 행의 어떤 값을 바꾸는지 식별 기준이 중요합니다.'),
      step('delete','4. Delete','딜리트는 저장된 데이터를 삭제하는 작업입니다. 실제 서비스에서는 명령 자체뿐 아니라 언제 무엇을 삭제할 수 있는지 업무 정책도 함께 고려해야 합니다. 구체적인 삭제 명령과 안전한 사용법은 후속 장에서 다룹니다.')
    ]
  });

  // 39페이지: SQL 구조 읽기와 실행 경계를 두 단계로 고정합니다.
  apply('part10-03', {
    script: '이 페이지의 짧은 에스큐엘은 문법을 실행하기보다 대상 테이블과 선택 열을 구조적으로 읽는 예고용 예제입니다.',
    steps: [
      step('sql','1. SQL 구조 읽기','셀렉트 네임은 네임 열을 조회하겠다는 뜻이고 프롬 스튜던츠는 대상 테이블이 스튜던츠라는 뜻입니다. 따라서 전체 의미는 스튜던츠 테이블에서 네임 열을 선택한다는 것입니다. 지금은 이 대상과 역할만 읽을 수 있으면 충분합니다.'),
      step('boundary','2. 실행은 Chapter 04','이번 2장의 목표는 설치나 에스큐엘 실행이 아니라 데이터베이스의 기본 구조를 이해하는 것입니다. 테이블 생성과 데이터 입력, 조회, 수정, 삭제는 4장에서 실제로 실행합니다. 개념 소개와 실습 실행의 경계를 분명하게 구분합니다.')
    ]
  });

  // 40페이지: 네 테이블 각각의 단계 안에서 키 판단의 근거까지 포함합니다.
  apply('part11-01', {
    script: '온라인 강의 서비스의 네 테이블은 먼저 한 행의 의미를 고정한 뒤 기본키와 참조 관계를 판단합니다.',
    steps: [
      step('students','1. students','스튜던츠의 한 행은 학생 한 명을 나타냅니다. 이름과 이메일, 전공 같은 학생 자체의 속성을 한 학생 행에 둡니다. 이 의미가 분명해야 학생 행을 구분할 기본키도 자연스럽게 찾을 수 있습니다.'),
      step('instructors','2. instructors','인스트럭터즈의 한 행은 강사 한 명을 나타냅니다. 강사 정보는 강의나 수강신청과 다른 성격의 데이터이므로 별도의 업무 대상으로 관리합니다. 강사 자체와 강사가 담당하는 강의 관계를 구분합니다.'),
      step('courses','3. courses','코시즈의 한 행은 강의 한 개를 나타냅니다. 강의 제목이나 담당 강사처럼 강의 자체를 설명하는 정보를 저장합니다. 특정 학생이 이 강의를 신청했다는 사실은 코시즈가 아니라 별도의 신청 사건으로 표현합니다.'),
      step('enrollments','4. enrollments','인롤먼츠의 한 행은 수강신청 한 건을 나타냅니다. 학생과 강의가 연결되며 발생한 사건이므로 학생과 강의를 참조할 외래키가 필요해집니다. 네 테이블의 한 행 의미를 구분하면 기본키와 외래키의 위치도 훨씬 쉽게 판단할 수 있습니다.')
    ]
  });

  // 41페이지: 화면의 다섯 질문과 정확히 1:1로 이동합니다.
  apply('part11-02', {
    body: `<div style="display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:14px">
      ${numberedCard('row-meaning','1','한 행의 의미','이 테이블의 한 행은 무엇을 나타내는가?','warn')}
      ${numberedCard('primary-key','2','기본키','각 행을 구분할 기본키가 있는가?')}
      ${numberedCard('reference','3','다른 행 참조','다른 테이블을 참조할 값은 무엇인가?')}
      ${numberedCard('mixing','4','데이터 혼합','서로 다른 성격의 데이터가 섞이지 않았는가?')}
      ${numberedCard('type-required','5','타입·필수 여부','값의 의미와 규칙이 맞는가?','good')}
    </div><div class="quote" style="margin-top:18px;font-size:25px">문법적으로 실행되어도 구조가 올바르다는 뜻은 아닙니다. AI는 초안을 만들고, 사람은 업무 의미와 연결 규칙을 검증합니다.</div>`,
    script: '에이아이가 만든 데이터베이스 구조는 문법보다 먼저 한 행의 의미부터 타입과 필수 여부까지 다섯 질문으로 순서대로 검토합니다.',
    steps: [
      step('row-meaning','1. 한 행의 의미','첫째, 이 테이블의 한 행이 무엇을 나타내는지 한 문장으로 설명합니다. 한 행의 의미가 흔들리면 서로 다른 종류의 데이터가 한 테이블에 섞였을 가능성이 있습니다. 구조 검토의 출발점은 항상 한 행의 업무 의미입니다.'),
      step('primary-key','2. 기본키','둘째, 각각의 행을 안정적으로 구분할 기본키가 있는지 확인합니다. 이름처럼 중복되거나 변경될 수 있는 값만으로 행을 구분하려 한다면 구조를 다시 검토해야 합니다. 기본키는 자신의 테이블에서 한 행을 안정적으로 식별해야 합니다.'),
      step('reference','3. 다른 행 참조','셋째, 다른 테이블을 참조해야 할 값이 무엇인지 확인합니다. 학생이나 강의처럼 이미 별도 테이블에서 관리하는 대상을 연결해야 한다면 외래키 참조를 고려합니다. 이름이나 제목을 반복하기보다 어떤 행을 가리키는지 키로 표현하는 것이 핵심입니다.'),
      step('mixing','4. 데이터 혼합','넷째, 서로 다른 성격의 데이터가 한 테이블에 불필요하게 섞이지 않았는지 확인합니다. 학생 이름이나 강의 제목처럼 다른 대상의 정보를 반복 저장하면 수정할 곳이 늘고 값이 서로 달라질 위험이 커집니다. 한 행의 의미와 맞지 않는 정보가 섞였다면 분리 여부를 검토합니다.'),
      step('type-required','5. 타입과 필수 여부','다섯째, 데이터 타입과 필수 여부가 실제 값의 의미에 맞는지 확인합니다. 숫자처럼 보인다고 무조건 계산용 숫자로 저장하거나 모든 값을 반드시 입력하게 만드는 것이 항상 올바른 것은 아닙니다. 다섯 질문을 모두 통과한 뒤에 실행 성공 여부를 확인합니다.')
    ]
  });

  // 42페이지: 활동 장표는 문제 전체 → 해설 공개 한 번으로 고정합니다.
  apply('part11-03', {
    script: '인롤먼츠 초안은 다섯 질문으로 한 번에 검토하고 정답 공개 단계에서 발견한 문제를 묶어서 확인합니다.',
    steps: [
      step('answer','정답 공개','한 행은 수강신청 한 건이어야 하며 아이디는 그 신청 행의 기본키 후보입니다. 스튜던트 아이디와 코스 아이디는 학생과 강의를 참조할 외래키 후보이고 학생 이름, 강의 제목, 강사 이름은 다른 대상의 정보가 반복되어 섞였는지 검토합니다. 프라이스는 신청 당시 금액인지 결제 완료 금액인지 시점과 의미가 모호하므로 업무 의미를 더 명확히 정의해야 합니다.')
    ]
  });

  // 44페이지: 네 단계 안에서 전체 요약까지 마칩니다.
  apply('closing-02', {
    script: 'Chapter 02의 전체 지도는 요청 경로, 저장 계층, 테이블의 의미와 키, 마지막 사람의 검증이라는 네 단계로 연결됩니다.',
    steps: [
      step('request','1. 요청 경로','사용자는 디비버 같은 클라이언트에서 에스큐엘을 작성하고 클라이언트는 그 명령을 포스트그레스큐엘에 전달합니다. 디비엠에스는 요청을 처리해 결과나 오류를 돌려줍니다. 사용자와 클라이언트, 디비엠에스의 역할을 섞지 않습니다.'),
      step('storage','2. 저장 계층','포스트그레스큐엘 안에서는 데이터베이스, 스키마, 테이블의 위치를 차례로 찾습니다. 현재 어느 서버의 어느 데이터베이스와 스키마를 보는지 알아야 오류도 정확하게 해석할 수 있습니다. 저장 계층은 데이터가 어디에 놓이는지를 보여 주는 지도입니다.'),
      step('meaning','3. 행·열과 키','테이블에서는 먼저 한 행이 무엇을 나타내는지 확인하고 열과 셀의 의미를 읽습니다. 기본키는 자신의 행을 구분하고 외래키는 다른 테이블의 행을 참조합니다. 에스큐엘은 이 구조를 조회하고 변경하는 언어입니다.'),
      step('human','4. 사람의 검증','마지막에는 사람이 결과와 구조의 업무 의미를 다시 검증합니다. 화면에 결과가 나왔거나 에이아이 초안이 실행되었다는 사실만으로 올바르다고 판단하지 않습니다. 한 행의 의미, 키, 참조, 데이터 혼합, 타입과 필수 여부를 확인하면서 전체 흐름을 마칩니다.')
    ]
  });

  // 45페이지: 설치 → 연결 → 현재 위치 확인의 세 단계 안에서 장을 마칩니다.
  apply('closing-03', {
    script: '다음 3장에서는 이번 장에서 만든 구조 지도를 실제 포스트그레스큐엘과 디비버 설치, 연결, 위치 확인 화면에 연결합니다.',
    steps: [
      step('install','1. 설치','먼저 포스트그레스큐엘 디비엠에스와 디비버 클라이언트를 각각 설치합니다. 이번 장에서 두 프로그램의 역할을 구분했기 때문에 어떤 프로그램이 데이터를 관리하고 어떤 프로그램이 명령을 전달하는지 실제 화면과 연결할 수 있습니다. 설치 단계에서도 두 도구를 같은 것으로 보지 않습니다.'),
      step('connect','2. 연결','다음으로 디비버에서 연결 정보를 설정해 포스트그레스큐엘 서버에 접속합니다. 호스트와 포트, 데이터베이스, 사용자 정보가 클라이언트에서 서버까지 실제 요청 경로를 만듭니다. 연결에 성공하면 이번 장의 요청 흐름이 실제 프로그램으로 보이기 시작합니다.'),
      step('confirm','3. 현재 위치 확인','마지막으로 에이아이 데이터베이스 북 데이터베이스를 만들고 현재 데이터베이스와 스키마를 확인합니다. 오늘은 설치 절차를 미리 실행하지 않고 구조와 용어를 먼저 익혔습니다. 이제 이 구조 지도를 가지고 3장에서 실제 화면과 에스큐엘로 현재 위치를 확인합니다.')
    ]
  });

  window.CH2SemanticOverrides = Object.freeze({
    version: '20260808-semantic-alignment-1',
    slideIds: [
      'part03-01','part04-01','part05-01','part06-01','part06-04','part07-02','part07-03',
      'part08-01','part08-02','part08-04','part09-01','part09-02','part09-03','part10-02',
      'part10-03','part11-01','part11-02','part11-03','closing-02','closing-03'
    ]
  });
})();
