-- Chapter 15. 실전 프로젝트 2 - seed.sql
-- 목적: 테이블 관계, 상태와 집계 결과를 확인할 수 있는 가상 샘플 데이터를 입력한다.
-- 실행 순서: schema.sql → seed.sql → queries.sql
-- 주의: 실제 개인정보나 운영 데이터를 사용하지 않는다.

-- ============================================================
-- 1. 예제 사용자 데이터
-- ============================================================

INSERT INTO example_users (name, email, role)
VALUES
    ('김하늘', 'haneul@example.com', 'user'),
    ('이바다', 'bada@example.com', 'manager'),
    ('박별', 'byeol@example.com', 'user'),
    ('최구름', 'gureum@example.com', 'user');

-- ============================================================
-- 2. 예제 항목 데이터
-- ============================================================

INSERT INTO example_items (title, description, status)
VALUES
    ('활성 항목 A', '여러 사용자와 연결되는 항목', 'active'),
    ('활성 항목 B', '한 사용자와 연결되는 항목', 'active'),
    ('비활성 항목', '상태 필터를 확인하기 위한 항목', 'inactive'),
    ('연결되지 않은 항목', 'LEFT JOIN과 누락 검증용 항목', 'archived');

-- ============================================================
-- 3. N:M 관계 데이터
-- ============================================================

INSERT INTO example_user_items (user_id, item_id, relation_role)
VALUES
    (1, 1, 'owner'),
    (1, 2, 'member'),
    (2, 1, 'member'),
    (2, 3, 'owner'),
    (3, 1, 'viewer');

-- 사용자 4번은 어떤 항목과도 연결하지 않는다.
-- 항목 4번도 어떤 사용자와도 연결하지 않는다.
-- 이 데이터는 LEFT JOIN과 누락 검증에 사용한다.

-- ============================================================
-- 4. 입력 결과 확인
-- ============================================================

SELECT COUNT(*) AS example_user_count
FROM example_users;

SELECT COUNT(*) AS example_item_count
FROM example_items;

SELECT COUNT(*) AS example_relation_count
FROM example_user_items;

-- ============================================================
-- 5. 실제 프로젝트 작성 영역
-- ============================================================
-- 아래에 프로젝트 주제에 맞는 INSERT를 작성한다.
-- 고정된 숫자 PK를 직접 참조할 때는 schema.sql이 초기화된 환경인지 확인한다.

-- INSERT INTO ...

-- ============================================================
-- 6. 샘플 데이터 점검
-- ============================================================
-- 1. 각 핵심 테이블에 데이터를 입력했는가?
-- 2. 1:N 또는 N:M 관계가 여러 건으로 표현되는가?
-- 3. 상태값이 서로 다른 데이터가 있는가?
-- 4. JOIN에서 포함되지 않는 부모 데이터를 확인할 수 있는가?
-- 5. 집계 결과에 차이를 만드는 데이터가 있는가?
-- 6. 정상값뿐 아니라 경계 상황을 확인할 수 있는가?
-- 7. 실제 개인정보, 비밀번호와 API 키가 포함되지 않았는가?
-- 8. 오류를 유도하는 INSERT는 기본 흐름과 분리해 주석 처리했는가?

-- 오류 검증 예시: UNIQUE 위반 확인용이며 필요할 때 한 줄씩 실행한다.
-- INSERT INTO example_users (name, email, role)
-- VALUES ('중복 사용자', 'haneul@example.com', 'user');

-- 오류 검증 예시: CHECK 위반 확인용이며 필요할 때 한 줄씩 실행한다.
-- INSERT INTO example_items (title, status)
-- VALUES ('잘못된 상태 항목', 'unknown');
