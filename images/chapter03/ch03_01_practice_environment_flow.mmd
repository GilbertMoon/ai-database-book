flowchart TB
    H[학습자<br/>요구사항을 이해하고 실행·검증을 책임]

    subgraph GUIDE[1. 학습·작성 보조]
        B[Chapter 03 원고<br/>설치·연결·실습 절차와 판단 기준]
        A1[ChatGPT<br/>개념 설명·오류 메시지 해석·검토 질문 보조]
        A2[Codex<br/>SQL·문서 파일 초안 작성과 수정 보조]
    end

    H --> B
    H --> A1
    H --> A2

    subgraph WORK[2. 로컬 작업 공간과 GitHub 저장소]
        W[프로젝트 폴더]
        S[setup_check.sql 등 SQL 파일<br/>재실행 가능한 명령과 주석]
        D[README·실습 기록<br/>환경 조건·실행 순서·검증 결과·오류 해결 과정]
        G[GitHub<br/>SQL·문서의 버전과 변경 이유 관리]
        W --> S
        W --> D
        S --> G
        D --> G
    end

    B --> W
    A2 --> W

    subgraph CLIENT[3. 데이터베이스 클라이언트]
        C[DBeaver<br/>연결 설정·SQL 편집·실행 요청·결과와 오류 표시]
        K[접속 정보<br/>host·port·database·user·password]
        K --> C
    end

    S --> C

    subgraph SERVER[4. 실제 데이터베이스]
        P[(PostgreSQL 서버<br/>로컬 또는 클라우드<br/>데이터 저장·SQL 처리·제약조건·트랜잭션)]
    end

    C -->|연결 후 SQL 실행 요청| P
    P -->|조회 결과·영향받은 행 수·오류| C

    C --> V[사람이 결과 검증<br/>연결 성공·현재 DB·테이블 구조·샘플 데이터·예상 오류 확인]
    V --> Q{예상한 결과와<br/>일치하는가?}
    Q -- 아니요 --> E[오류 정보 정리<br/>실행 SQL·전체 오류 메시지·환경·재현 단계]
    E --> A1
    E --> S
    Q -- 예 --> R[검증 결과와 수정 이유 기록]
    R --> D

    N1[역할 구분<br/>PostgreSQL은 데이터를 실제로 보관하고 처리하며<br/>DBeaver는 서버에 연결하는 클라이언트일 뿐 데이터를 대신 보관하지 않음] --- P
    N2[보안<br/>비밀번호·전체 접속 문자열·비밀키는 SQL·README·공개 GitHub·AI 질문에 기록하지 않음] --- K
    N3[AI 사용 원칙<br/>AI 답변과 생성 파일은 초안이며 실제 실행 결과와 공식 설정 화면으로 검증] --- A1
    N4[핵심<br/>읽기 → 파일 작성 → 연결 → 실행 → 결과 확인 → 수정·기록의 반복 가능한 환경을 만든다] --- V
