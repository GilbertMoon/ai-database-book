# Chapter 04 최종 출판 리뷰 체크리스트

## 대상 Chapter

```text
Chapter 04. 관계형 데이터베이스와 SQL 시작하기
```

## 리뷰 목적

Chapter 04가 SQL을 처음 배우는 일반 독자에게 필요한 기본 CRUD를 단계적으로 제공하고, 고급 PostgreSQL 기능과 내부 동작을 선택·심화 학습으로 분리했는지 점검한다.

---

## 1. 대상·범위·구조

| 점검 항목 | 상태 | 최종 반영 내용 |
| --- | --- | --- |
| 대상 독자 | 통과 | SQL을 처음 실행하는 독자 명시 |
| 선수 지식 | 통과 | Chapter 03 환경 외 별도 SQL 경험 없음 |
| 자율 학습 표현 | 통과 | 특정 수업 일정·평가·제출 표현 없음 |
| 학습 표시 | 통과 | 핵심·선택·심화 사용 |
| 권장 분량 | 통과 | 21~24페이지 |
| 본문 구조 | 통과 | 23개 절에서 17개 절로 정리 |
| 핵심 메시지 | 통과 | 예상과 실제 결과 비교 중심 |

---

## 2. Chapter 03 연결과 실행 위치

| 점검 항목 | 상태 | 최종 반영 내용 |
| --- | --- | --- |
| 현재 DB 확인 | 통과 | `current_database()` 사용 |
| 현재 사용자 확인 | 통과 | `current_user` 추가 |
| 스키마·검색 경로 | 통과 | `current_schema()`, `SHOW search_path` 유지 |
| `public` 절대 조건 제거 | 통과 | 현재 스키마는 환경에 따라 다를 수 있음 |
| 스키마 한정 이름 | 통과 | 주요 예제에서 `public.students` 사용 |
| 실행 범위 | 통과 | 현재 문장·선택 영역 확인 |
| Auto-commit | 통과 | 공통 실행 원칙에서 안내 |

---

## 3. SQL 기본 작성 규칙

| 점검 항목 | 상태 | 최종 반영 내용 |
| --- | --- | --- |
| 키워드 표기 | 통과 | 책에서는 대문자 사용 |
| 식별자 표기 | 통과 | 소문자 `snake_case` 사용 |
| 문자열 | 통과 | 작은따옴표 사용 |
| 숫자 | 통과 | 따옴표 없이 작성 |
| 세미콜론 | 통과 | 문장 끝 사용 |
| 주석 | 통과 | `--` 한 줄 주석 설명 |
| 큰따옴표 | 통과 | 식별자 용도이며 초급 예제에서 회피 |

---

## 4. 테이블과 데이터 타입

| 점검 항목 | 상태 | 최종 반영 내용 |
| --- | --- | --- |
| 첫 테이블 | 통과 | `public.students` 유지 |
| 핵심 타입 | 통과 | `INTEGER`, `VARCHAR`, `TIMESTAMPTZ` |
| 타입 범주 보강 | 통과 | `BIGINT`, `NUMERIC`, `TEXT`, `BOOLEAN`, `DATE` 추가 |
| 금액 타입 | 통과 | 정확성이 중요하면 `NUMERIC` 우선 검토 |
| 제약조건 | 통과 | PK, NOT NULL, UNIQUE, DEFAULT 기본 설명 |
| 형 변환 | 통과 | `CAST()`와 PostgreSQL `::` 선택 학습 |
| 추가 타입 | 통과 | `UUID`, `JSONB` 선택 미리보기 |
| 교육용 구조 | 통과 | 상세 업무 규칙은 Chapter 06 연결 |

---

## 5. IDENTITY와 시간 설명

| 점검 항목 | 상태 | 최종 반영 내용 |
| --- | --- | --- |
| 내부 식별자 | 통과 | 학생 수·학번과 구분 |
| 번호 공백 | 통과 | 핵심 한 문장, 상세는 심화 학습 |
| `CURRENT_TIMESTAMP` | 통과 | 기본 등록 시각으로 설명 |
| 트랜잭션 시각 | 통과 | 상세 동작은 심화 학습 |
| 최신 정렬 | 통과 | `created_at`, `id` 보조 기준 사용 |

---

## 6. INSERT와 SELECT

| 점검 항목 | 상태 | 최종 반영 내용 |
| --- | --- | --- |
| 단일 행 입력 | 통과 | 김민지 예제 |
| 여러 행 입력 | 통과 | 4명 다중 VALUES |
| 선택값 생략 | 통과 | 윤서진 NULL 예제 |
| `RETURNING` | 통과 | PostgreSQL 선택 기능으로 표시 |
| 필요한 열 선택 | 통과 | `SELECT` 목록 설명 |
| 별칭 | 통과 | `AS` 선택 학습 |
| 결과 순서 | 통과 | `ORDER BY` 없으면 미보장 |

---

## 7. 조건·문자열·NULL

| 점검 항목 | 상태 | 최종 반영 내용 |
| --- | --- | --- |
| 비교 연산자 | 통과 | `=`, `<>`, `>`, `>=`, `<`, `<=` |
| 논리 연산 | 통과 | `AND`, `OR`, `IN`, 괄호 |
| 패턴 검색 | 통과 | `LIKE`, `%`, `_` |
| `ILIKE` | 통과 | PostgreSQL 선택 기능으로 표시 |
| NULL 기본 | 통과 | 0·빈 문자열과 구분 |
| NULL 확인 | 통과 | `IS NULL`, `IS NOT NULL` |
| `= NULL` | 통과 | 원하는 결과를 반환하지 않는 이유 설명 |
| `<>`와 NULL | 통과 | NULL 행 제외 설명 |
| 3값 논리 | 통과 | `UNKNOWN`은 심화 학습 |
| `IS DISTINCT FROM` | 통과 | 선택·심화 문법으로 이동 |

---

## 8. DISTINCT·정렬·LIMIT

| 점검 항목 | 상태 | 최종 반영 내용 |
| --- | --- | --- |
| `DISTINCT` | 통과 | 전공 중복 제거 예제 추가 |
| 원본 변경 오해 | 통과 | 조회 결과만 중복 제거 |
| 기본 정렬 | 통과 | `ORDER BY grade, id` |
| NULL 위치 | 통과 | `NULLS LAST` 선택 학습 |
| LIMIT | 통과 | 정렬 후 개수 제한 |
| 동률 처리 | 통과 | `id` 보조 기준 사용 |

---

## 9. UPDATE와 DELETE 안전성

| 점검 항목 | 상태 | 최종 반영 내용 |
| --- | --- | --- |
| 변경 전 SELECT | 통과 | 같은 조건으로 대상 확인 |
| `RETURNING` | 통과 | 실제 변경·삭제 행 확인 |
| 영향 행 수 | 통과 | 예상값과 비교 |
| 변경 후 SELECT | 통과 | 실제 상태 재확인 |
| WHERE 없는 UPDATE | 통과 | 전체 변경 위험 예시 |
| WHERE 없는 DELETE | 통과 | 전체 삭제 위험 예시 |
| 여러 열 UPDATE | 통과 | 선택 학습·기본 미실행 |
| Auto-commit 반복 | 통과 | 각 절 반복 대신 공통 경고 |

---

## 10. 데이터 상태와 자기 확인

| 점검 항목 | 상태 | 최종 반영 내용 |
| --- | --- | --- |
| 초기 상태 | 통과 | 학생 6명, 이준호 3, 박서연 존재 |
| 수정 후 상태 | 통과 | 학생 6명, 이준호 4 |
| 삭제 후 상태 | 통과 | 학생 5명, 박서연 삭제 |
| 체크포인트 문자 제거 | 통과 | 상태를 자연어로 표현 |
| 문제 기준 | 통과 | 초기 데이터 상태 사용 |
| 복원 절차 | 통과 | reset → create → insert → verify |

---

## 11. AI SQL 검토

| 점검 항목 | 상태 | 최종 반영 내용 |
| --- | --- | --- |
| 검토 질문 수 | 통과 | 핵심 여섯 질문으로 단순화 |
| 연결·구조 | 통과 | 실제 DB·테이블·열 확인 |
| 조건·NULL | 통과 | 요구사항과 비교 |
| 영향 범위 | 통과 | 예상 반환·변경 행 수 확인 |
| 사전 SELECT | 통과 | 변경 전 동일 조건 조회 |
| 실제 결과 | 통과 | 예상과 실행 결과 비교 |
| 위험 예제 | 통과 | 컴퓨터공학 DELETE는 실행 금지 |

---

## 12. SQL 파일 구조

| 파일 | 상태 | 최종 역할 |
| --- | --- | --- |
| `01_create_students.sql` | 통과 | 위치·상태 확인 후 테이블 생성 |
| `02_insert_students.sql` | 통과 | 빈 테이블 확인 후 학생 6명 입력 |
| `03_select_students.sql` | 통과 | 반복 가능한 조회 실습 |
| `04_update_delete_students.sql` | 통과 | 초기 상태 확인 후 수정·삭제 |
| `verify_students.sql` | 통과 | 구조와 데이터 상태 반복 조회 |
| `reset_students.sql` | 통과 | DB·public·쓰기 상태 확인 후 삭제 |
| `basic_crud.sql` | 통과 | 기존 링크용 통합 참고 파일 |
| 코드 README | 통과 | 시작·완료 상태와 순서 문서화 |

---

## 13. 워크북과 도식

| 점검 항목 | 상태 | 최종 반영 내용 |
| --- | --- | --- |
| 워크북 구분 | 통과 | 핵심·선택·심화 활동 |
| 번호 파일 연결 | 통과 | 실행 순서와 복원 절차 반영 |
| 예상·실제 기록 | 통과 | 주요 SQL마다 비교 표 제공 |
| 도식 8종 | 통과 | CRUD·필터·정렬·안전 실행 유지 |
| 고급 문법 도식화 | 통과 | 코드·표로 설명하고 도식 과밀 방지 |

---

## 14. 최종 판정

```text
Chapter 04는 첫 SQL 실습에 필요한 핵심 흐름을 유지하면서
SQL 작성 규칙, 데이터 타입, 형 변환과 DISTINCT를 보강했다.

고급 PostgreSQL 문법과 내부 동작은 선택·심화 학습으로 분리했고,
번호 SQL 파일과 상태 확인 파일을 통해 반복 실습의 안전성을 높였다.
```

실제 PostgreSQL 통합 실행과 Word·PDF·eBook 렌더링은 전체 제작 단계에서 별도로 확인한다.
