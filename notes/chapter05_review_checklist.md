# Chapter 05 리뷰 체크리스트

## 대상 Chapter

```text
Chapter 05. 데이터 모델링과 ERD
```

## 리뷰 목적

Chapter 05가 요구사항 분석, 엔터티·속성 구분, 관계 설계, ERD 작성, PostgreSQL 구현, 샘플 데이터 검증, AI 생성 ERD 검토 흐름을 초보자 수준에서 일관되게 설명하는지 점검합니다.

---

## 1. 구조 리뷰

| 점검 항목 | 상태 | 리뷰 의견 |
| --- | --- | --- |
| 이 장에서 다룰 내용이 명확한가 | 통과 | 데이터 모델링, ERD, PK/FK, 관계, AI ERD 검토 목표가 제시됨 |
| 데이터 모델링 단계별 산출물 표가 있는가 | 통과 | 2절에 단계·핵심 질문·산출물 표 추가 |
| 예제가 일관적인가 | 통과 | `members`, `books`, `loans` 예제를 전체 장에서 유지 |
| 실습 SQL과 연결되는가 | 통과 | `code/chapter05/library_schema.sql`을 실행 기준으로 명시 |
| 활동 자료가 본문과 일치하는가 | 통과 | 테이블, 컬럼, 3명·3권·4건 데이터, JOIN 결과를 동기화 |
| 다음 장과 연결되는가 | 통과 | Chapter 06 정규화 연결 문장 유지 |

---

## 2. 내용 정확성 리뷰

| 점검 항목 | 상태 | 리뷰 의견 |
| --- | --- | --- |
| 엔터티와 속성 기준 | 통과 | 독립 관리 대상, 사건, 설명 값으로 구분 |
| `loans` 역할 | 통과 | 두 FK와 날짜 속성을 가진 사건·업무 테이블로 설명 |
| PK와 FK 방향 | 통과 | 자식 FK에서 부모 PK를 참조하도록 통일 |
| 1:N 관계 | 통과 | N쪽 `loans`에 FK가 위치함을 설명 |
| N:M 관계 | 통과 | `loans`로 두 개의 1:N 관계로 해소 |
| NULL 허용 | 통과 | `returned_at`의 미반납 상태를 NULL로 표현 |
| UNIQUE | 통과 | `members.email`, `books.isbn`에 일관되게 반영 |
| 범위 가정 | 통과 | 복본 구분은 Chapter 05 범위에서 제외한다고 한 번만 명시 |

---

## 3. 본문·SQL·활동 자료 정합성

| 점검 항목 | 상태 | 리뷰 의견 |
| --- | --- | --- |
| DDL 구조 일치 | 통과 | 세 파일 모두 같은 테이블과 컬럼 사용 |
| 샘플 데이터 일치 | 통과 | 회원 3명, 도서 3건, 대여 4건으로 통일 |
| JOIN 예상 결과 | 통과 | 본문과 활동 자료에서 4행으로 통일 |
| 미반납 결과 | 통과 | `returned_at IS NULL`, 3행으로 통일 |
| 오류 SQL | 통과 | 존재하지 않는 FK 입력 예시는 주석 처리 유지 |
| 고급 범위 추가 여부 | 통과 | ON DELETE, 복합 UNIQUE, 복본 테이블, 인덱스 등을 추가하지 않음 |

---

## 4. 도식 리뷰

| 점검 항목 | 상태 | 리뷰 의견 |
| --- | --- | --- |
| SVG 8개 단순화 | 통과 | 각 도식이 하나의 핵심 질문에 답하도록 재구성 |
| Mermaid 원본 동기화 | 통과 | SVG의 핵심 노드·관계·분기와 일치 |
| 대형 표·전체 SQL 제거 | 통과 | 상세 정보는 본문 표와 SQL 코드로 이동 |
| 접근성 구조 | 통과 | title, desc, role, aria-labelledby 포함 |
| 안전한 폰트 스택 | 통과 | 한글·코드 폰트 스택 반영 |
| XML 파싱 | 통과 | 8개 SVG XML 파싱 성공 |
| Mermaid CLI 문법 검증 | 미실행 | 실행 환경에 Mermaid CLI가 없어 정적 검토만 수행 |
| GitHub 실제 렌더링 | 수동 확인 필요 | 브랜치/PR 화면에서 최종 확인 필요 |
| PDF/Word/eBook 렌더링 | 수동 확인 필요 | 실제 변환 파이프라인에서 확인 필요 |

---

## 5. 본문·표·이미지 역할 분리

| 매체 | 담당 역할 | 상태 |
| --- | --- | --- |
| 본문 | 요구사항 해석과 설계 판단 근거 | 통과 |
| Markdown 표 | 단계별 산출물, 상세 대응, 검토 질문 | 통과 |
| SQL 코드 | 실행 가능한 DDL, 데이터, JOIN, 오류 검증 | 통과 |
| Mermaid | SVG의 의미적·구조적 원본 | 통과 |
| SVG | 흐름과 관계의 핵심 구조 | 통과 |

---

## 6. 최종 판정

```text
Chapter 05의 본문, 활동 자료, SQL, Mermaid, SVG, 이미지 README, 리뷰 기록을 정합성 기준으로 보정했다.
개념과 파일 간 정합성 검증은 완료했으며, GitHub·Word·PDF·eBook의 실제 렌더링은 수동 확인이 필요하다.
```
