# Chapter 05 리뷰 체크리스트

## 대상 Chapter

```text
Chapter 05. 요구사항에서 데이터 모델과 ERD 만들기
```

## 리뷰 목적

Chapter 05가 요구사항을 업무 사실, 속성, 관계와 규칙으로 분해하고, 확정·미확정 정책을 구분하며, 관계 문장·카디널리티·선택성·PK·FK·ERD·요구사항 추적표로 이어지는 흐름을 초보자 수준에서 일관되게 설명하는지 점검합니다.

---

## 1. 구조 리뷰

| 점검 항목 | 상태 | 리뷰 의견 |
| --- | --- | --- |
| 장의 역할이 명확한가 | 통과 | 요구사항 근거와 관계 판단 중심으로 재구성 |
| Chapter 04와 연결되는가 | 통과 | 이미 정해진 테이블 실행에서 직접 설계로 전환 |
| Chapter 06과 범위가 구분되는가 | 통과 | 정규화·FK 오류·무결성 상세를 Chapter 06으로 이동 |
| Chapter 08과 범위가 구분되는가 | 통과 | 최소 관계 확인 JOIN만 유지 |
| 본문·워크북·SQL 파일이 연결되는가 | 통과 | schema, seed, validation, reset 역할 분리 |
| 다음 장과 연결되는가 | 통과 | 요구사항·ERD·추적표를 Chapter 06 검토 기준으로 사용 |

---

## 2. 요구사항 분석 리뷰

| 점검 항목 | 상태 | 리뷰 의견 |
| --- | --- | --- |
| 업무 사실과 업무 규칙 구분 | 통과 | 관리 대상·속성·관계 규칙을 구분 |
| 확정·미확정 규칙 구분 | 통과 | 요구사항에 없는 정책을 질문 목록으로 분리 |
| 범위 가정 명시 | 통과 | 복본, 동시 대여와 연체 정책을 범위 밖으로 기록 |
| 명사 찾기의 한계 설명 | 통과 | 동사와 수량 표현에서 관계를 찾도록 보완 |
| 요구사항 추적표 | 통과 | 요구사항과 테이블·열·검증 방법 연결 |
| AI 임의 가정 검토 | 통과 | 근거·가정·미확정 질문을 별도로 확인 |

---

## 3. 데이터 모델링 정확성

| 점검 항목 | 상태 | 리뷰 의견 |
| --- | --- | --- |
| 엔터티 판단 기준 | 통과 | 식별자, 속성, 반복 저장, 관계와 사건 여부 사용 |
| 엔터티·속성 구분 | 통과 | 회원·도서·대여와 설명 값을 구분 |
| 사건 엔터티 설명 | 통과 | `loans`를 날짜와 상태를 가진 업무 테이블로 설명 |
| 관계 문장 | 통과 | 회원·도서·대여 관계를 양방향으로 작성 |
| 1:N 관계 | 통과 | N쪽 `loans`에 FK가 위치함을 설명 |
| N:M 해소 | 통과 | `loans`를 통해 두 개의 1:N으로 표현 |
| 선택성 | 통과 | 부모→대여 `0..N`, 대여→부모 `1`로 설명 |
| PK와 업무상 고유값 | 통과 | ID와 email·ISBN의 역할을 구분 |
| NULL 허용 | 통과 | `returned_at`을 선택 속성으로 설명 |

---

## 4. 본문·SQL·워크북 정합성

| 점검 항목 | 상태 | 리뷰 의견 |
| --- | --- | --- |
| 테이블 구조 일치 | 통과 | `members`, `books`, `loans` 유지 |
| PK 방식 일치 | 통과 | 모든 테이블에 IDENTITY 사용 |
| 외래키 방향 일치 | 통과 | `loans`에서 부모 PK 참조 |
| 자동 DROP 제거 | 통과 | schema와 seed에는 삭제 구문 없음 |
| 초기화 파일 분리 | 통과 | `reset_library.sql` 별도 제공 |
| 샘플 ID 재현성 | 통과 | 101~103, 201~203, 1001~1004 명시 |
| 샘플 데이터 수 | 통과 | 회원 3, 도서 3, 대여 4 |
| 미반납 결과 | 통과 | `returned_at IS NULL` 3행 |
| FK 오류 예시 제거 | 통과 | Chapter 06으로 이동 |
| JOIN 범위 | 통과 | 관계 연결 확인용 최소 예제만 유지 |
| 워크북 역할 | 통과 | 요구사항 판단과 설계 근거 작성 중심 |

---

## 5. 코드 실행 구조

| 파일 | 점검 항목 | 상태 |
| --- | --- | --- |
| `library_schema.sql` | 현재 위치 확인과 테이블 생성만 수행 | 통과 |
| `library_seed.sql` | 명시적 ID 샘플 데이터만 입력 | 통과 |
| `library_validation.sql` | 행 수·NULL·1:N·최소 JOIN 확인 | 통과 |
| `reset_library.sql` | 자식→부모 순서로 명시적 초기화 | 통과 |
| `README.md` | 실행 순서와 안전 주의 설명 | 통과 |

실제 PostgreSQL 실행은 별도 환경에서 확인이 필요합니다.

---

## 6. 도식 리뷰

| 점검 항목 | 상태 | 리뷰 의견 |
| --- | --- | --- |
| SVG 8개 유지 | 통과 | 기존 핵심 관계와 흐름이 새 본문과 호환 |
| 그림 등장 순서 | 통과 | 새 본문 순서에 맞게 이미지 README 갱신 |
| 사건 테이블 의미 | 통과 | N:M 해소와 `loans` 역할을 본문과 연결 |
| PK·FK 방향 | 통과 | 기존 도식 의미 유지 |
| 요구사항 추적표 | 본문 처리 | 표 형태가 적합해 새 SVG를 추가하지 않음 |
| 선택성 설명 | 본문 처리 | 문장과 표로 보완 |
| XML·접근성 | 기존 통과 | title, desc, role, aria-labelledby 유지 |
| GitHub 렌더링 | 수동 확인 필요 | 저장소 화면에서 최종 확인 필요 |
| PDF/Word/eBook | 수동 확인 필요 | 변환 파이프라인에서 확인 필요 |

---

## 7. 범위 이동 확인

| 이동 내용 | 대상 Chapter | 상태 |
| --- | --- | --- |
| 정규화와 이상 현상 상세 | Chapter 06 | 반영 예정 |
| FOREIGN KEY 오류 실습 | Chapter 06 | 반영 예정 |
| 참조 무결성 | Chapter 06 | 반영 예정 |
| JOIN 문법과 다양한 결합 | Chapter 08 | 반영 예정 |

후속 장 수정 시 실제로 수용되었는지 다시 확인해야 합니다.

---

## 8. 최종 판정

```text
Chapter 05의 본문, 워크북, 구성안, 코드 구조, 이미지 문서와 리뷰 기록을 2차 재구성했다.
요구사항 근거, 관계 문장, 선택성, 추적표와 AI 가정 검토가 핵심 흐름으로 반영되었다.
실제 PostgreSQL 실행과 출판 렌더링은 수동 확인이 필요하다.
```
