# Chapter 08 전체 점검 체크리스트

## 대상

```text
Chapter 08. JOIN과 집계로 서비스 질문에 답하기
```

## 1. Chapter 07 인계

- [x] `course_project` 네 테이블을 그대로 사용한다.
- [x] `recorded_amount NUMERIC(12,0)`를 현재 금액 열로 사용한다.
- [x] 과거 `paid_amount` 표현을 Chapter 08 본문·워크북·발표자료·도식에서 제거했다.
- [x] 전체 신청 5건 / 590000을 기준으로 사용한다.
- [x] 활성 신청 3건 / 340000을 기준으로 사용한다.
- [x] 취소 제외 신청 이력 4건 / 440000을 기준으로 사용한다.
- [x] 상태 분포는 신청 2 / 수강중 1 / 완료 1 / 취소 1이다.
- [x] 1001 완료·1004 취소·1005 신청 상태와 각 기록 금액을 사전 검사한다.

## 2. `00_check_course_project.sql`

- [x] 현재 DB가 `ai_database_book`인지 확인한다.
- [x] students·instructors·courses·enrollments 존재를 확인한다.
- [x] `uq_course_enrollments_active` 존재를 확인한다.
- [x] `recorded_amount`가 `NUMERIC(12,0)`인지 확인한다.
- [x] 3 / 2 / 3 / 5 행 수를 확인한다.
- [x] 전체 평균 118000.00을 확인한다.
- [x] 상태별 2 / 1 / 1 / 1을 확인한다.
- [x] 활성 3 / 340000을 확인한다.
- [x] 취소 제외 4 / 440000을 확인한다.
- [x] 학생·강의·강사 고아 관계가 모두 0인지 확인한다.
- [x] 활성 중복 신청이 0인지 확인한다.
- [x] 기준이 다르면 예외로 중단한다.
- [x] 통과 시 `Chapter 08 prerequisite check passed`를 출력한다.

## 3. JOIN 설명과 SQL

- [x] 결과 한 행의 기준을 먼저 정한다.
- [x] `enrollments → students`, `enrollments → courses → instructors` 실제 FK 경로를 사용한다.
- [x] INNER JOIN 신청 결과가 5행임을 확인한다.
- [x] 1:N 반복을 오류 중복으로 오해하지 않도록 설명한다.
- [x] LEFT JOIN에서 취소 제외 신청이 없는 박서연을 유지한다.
- [x] 오른쪽 조건을 `ON`에 둘 때 학생 3명이 유지됨을 설명한다.
- [x] 같은 조건을 `WHERE`에 둘 때 학생 2명만 남는 차이를 설명한다.
- [x] JOIN 확장 NULL과 원본 NULL을 구분한다.
- [x] `LEFT JOIN ... IS NULL`과 `NOT EXISTS`가 모두 박서연 1명을 반환함을 검산한다.
- [x] RIGHT·FULL·CROSS JOIN은 현재 입문 범위 밖이라고 명시한다.

## 4. 집계 설명과 SQL

- [x] `COUNT(*)`, `COUNT(e.id)`, `COUNT(DISTINCT e.student_id)`의 의미를 구분한다.
- [x] 강의 303의 취소 제외 결과에서 `COUNT(*)=1`, `COUNT(e.id)=0`, 고유 학생=0을 설명한다.
- [x] `SUM·AVG·MIN·MAX`의 빈 입력 NULL 가능성을 설명한다.
- [x] `COALESCE(...,0)`는 업무 의미가 맞을 때만 사용한다.
- [x] `recorded_amount` 평균은 `ROUND(...,2)`로 표시한다.
- [x] 상태별 GROUP BY 건수 합이 5인지 검산한다.
- [x] 상태별 기록 금액 합이 590000인지 검산한다.
- [x] `FILTER`에서 신청·수강중·활성·완료·취소를 구분한다.
- [x] `WHERE`와 `HAVING`의 적용 시점을 구분한다.
- [x] 강의 301 = 2건 / 2명 / 200000을 검산한다.
- [x] 강의 302 = 2건 / 2명 / 240000을 검산한다.
- [x] 강의 303 = 실제 취소 제외 0건 / 0명을 검산한다.

## 5. 과대 집계

- [x] `강사 → 강의 → 신청`의 연속 1:N JOIN에서 강의 행이 반복됨을 설명한다.
- [x] 신청 JOIN 뒤 `SUM(c.price)`가 강의 가격을 과대 합산할 수 있음을 보여준다.
- [x] `SUM(DISTINCT c.price)`가 일반 해결책이 아님을 설명한다.
- [x] 강의 가격 합계는 강의 수준에서 계산하도록 안내한다.
- [x] 강사 201은 강의 2개 / 신청 4건 / 취소 제외 4건으로 검산한다.
- [x] 강사 202는 강의 1개 / 신청 1건 / 취소 제외 0건으로 검산한다.

## 6. `03_join_aggregation_validation.sql`

- [x] 읽기 전용 자동 완료 게이트로 구성한다.
- [x] 상세 신청 수와 상태별 그룹 합을 5로 비교한다.
- [x] 상세 금액과 강의별 그룹 합을 590000으로 비교한다.
- [x] 활성 상세·그룹을 3 / 340000으로 비교한다.
- [x] 취소 제외 상세·강의별을 4 / 440000으로 비교한다.
- [x] INNER JOIN 결과 5행을 확인한다.
- [x] 고아 관계 0 / 0 / 0을 확인한다.
- [x] anti-join 두 방식이 각각 1명을 반환하는지 확인한다.
- [x] ON 조건 학생 3명 / WHERE 조건 학생 2명을 확인한다.
- [x] 강의 301·302·303 집계 기준값을 확인한다.
- [x] 강사 201·202 집계 기준값을 확인한다.
- [x] 하나라도 다르면 예외로 중단한다.
- [x] 통과 시 `Chapter 08 join and aggregation validation passed`를 출력한다.

## 7. 조회 전용 안전성

- [x] `00`, `01`, `02`, `03`, 호환 파일에 데이터·스키마 변경 SQL이 없어야 한다.
- [x] Chapter 08 전체 경로를 PostgreSQL `READ ONLY` 트랜잭션에서도 실행할 수 있도록 구성한다.
- [x] 실행 전후 Chapter 07 기준 데이터가 변하지 않아야 한다.
- [x] 기준 상태를 일부 바꾸면 `00`이 실패해야 한다.
- [x] 집계 금액을 일부 바꾸면 `03`이 실패해야 한다.

## 8. 본문·구성안·워크북

- [x] 본문 23개 절 흐름을 유지한다.
- [x] SQL 예제를 모두 현재 `recorded_amount` 구조와 맞춘다.
- [x] `recorded_amount`를 실제 결제 승인액·환불액·회계 매출로 단정하지 않는다.
- [x] 전체·활성·취소 제외 범위를 반복해서 구분한다.
- [x] 결과 한 행·JOIN 경로·조건 위치·집계 대상을 문법보다 먼저 결정하도록 안내한다.
- [x] `AVG` 설명을 현재 `NUMERIC(12,0)` 구조와 맞춘다.
- [x] `03` 자동 완료 게이트와 통과 메시지를 본문·실습 발표자료에 연결한다.

## 9. 발표자료·스크립트

- [x] 이론 강의안은 20개 절이다.
- [x] 실습 강의안은 20개 절이다.
- [x] 모든 절에 `화면 구성`과 `발표 스크립트`가 있다.
- [x] 실습 강의안에서도 실제 열 이름 `recorded_amount`를 보여준다.
- [x] `chapter08_slides.js`는 Markdown 원본을 직접 로드한다.
- [x] 의미 단위 내비게이션을 유지한다.
- [x] 발표자 스크립트와 장표의 postMessage 동기화를 유지한다.
- [x] 공통 `tts_pronunciation.js`를 유지한다.
- [x] 공통 `script_content_enhancer.js`를 유지한다.
- [x] 발표자료 자산 버전을 `20260809a`로 통일한다.
- [x] 스크립트 창에서 새 발표 창을 열 때 같은 자산 버전을 전달한다.

## 10. 이미지·도식

- [x] Mermaid 8개와 SVG 8개의 파일 쌍을 유지한다.
- [x] 현재 본문은 핵심 도식 4종을 사용한다.
- [x] JOIN 핵심 도식 01·03의 과거 `paid_amount`를 `recorded_amount`로 수정한다.
- [x] 집계 도식 06의 `결제금액` 표현을 `기록 금액`으로 수정한다.
- [x] SVG는 `width="100%"`, `viewBox`, `role="img"`, `title`, `desc`를 유지한다.
- [ ] 실제 브라우저·Word·PDF·eBook 가독성은 최종 출력 단계에서 확인한다.

## 11. 자동 검증

`.github/workflows/validate-chapter08.yml`에서 다음을 자동 검사한다.

- [x] Chapter 08 JavaScript 문법
- [x] 본문·구성안·워크북·발표자료의 핵심 기준값
- [x] Chapter 08 범위의 `paid_amount` 잔존 여부
- [x] 이론 20 / 실습 20 구조
- [x] 발표 자산 버전·TTS·스크립트 강화 연결
- [x] 조회 SQL의 DML·DDL 부재
- [x] 8개 Mermaid/SVG 쌍과 SVG 접근성 속성
- [x] PostgreSQL 16에서 Chapter 07 기준 상태 생성
- [x] 잘못된 DB에서 `00` 차단
- [x] Chapter 08 전체 경로를 READ ONLY 트랜잭션에서 실행
- [x] 실행 전후 데이터 불변 확인
- [x] `00` 기준 상태 드리프트 차단
- [x] `03` 집계 드리프트 차단
- [x] 최종 5 / 590000 / 3 / 340000 / 4 / 440000 확인

실제 GitHub Actions 실행 결과는 `notes/chapter08_validation_result.md`에 별도 기록한다.

## 12. 수동 확인으로 남는 항목

- [ ] 브라우저에서 이론/실습 20장 최종 렌더링
- [ ] 단계별 강조와 발표자 창 동기화 실제 조작
- [ ] TTS 실제 청취
- [ ] 모바일·프로젝터 화면 가독성
- [ ] Word·PDF·eBook 표·코드·SVG 최종 렌더링
